# Лабораторная работа №8 - Чтение и запись файлов

## 📋 Описание задания

Программа читает файл со словами, которые имеют случайную капитализацию, и создает новый файл с обработанными данными:
- Разделяет слова на те, что начинаются с гласных и согласных букв
- Выравнивает капитализацию (первая буква заглавная, остальные строчные)
- Записывает результат в новый файл

### Пример

**Входной файл (`input.txt`):**
```
яНВарь ФевРАль МАРТ АпРЕЛЬ маЙ ИЮнь июЛЬ авГУСТ СЕнТЯбРь ОКТЯБРЬ ноябрь дЕкАбРь
```

**Выходной файл (`output.txt`):**
```
Слова, начинающиеся с гласных:
Январь, Апрель, Июнь, Июль, Август, Октябрь
Слова, начинающиеся с согласных:
Февраль, Март, Май, Сентябрь, Ноябрь, Декабрь
```

---

## 🚀 Как запустить программу

### Вариант 1: Из Visual Studio
1. Откройте проект в Visual Studio
2. Нажмите **Ctrl+F5** (Запуск без отладки) или **F5** (С отладкой)
3. Программа создаст файлы в папке `x64\Debug\` или `Debug\`

### Вариант 2: Из скомпилированного .exe
1. Найдите файл `лаба8.exe` в папке проекта
2. Поместите рядом с ним файл `input.txt` с данными
3. Запустите программу двойным кликом
4. Результат появится в файле `output.txt`

---

## 📁 Структура файлов

```
проект/
│
├── лаба8.cpp          # Исходный код программы
├── input.txt          # Входной файл с данными (создается автоматически)
├── output.txt         # Выходной файл с результатом
└── README.md          # Этот файл
```

---

## 🔧 Как работает программа

### 1. Чтение файла
```cpp
ifstream inputFile("input.txt");
string line;
getline(inputFile, line);
```
Программа читает весь текст из `input.txt` в одну строку.

### 2. Конвертация кодировки
```cpp
wstring wline = stringToWstring(line);
```
Преобразует из Windows-1251 (`string`) в Unicode (`wstring`) для корректной работы с русскими буквами.

### 3. Разбиение на слова
```cpp
wstringstream ss(wline);
while (ss >> word) {
    words.push_back(word);
}
```
Разделяет строку на отдельные слова по пробелам.

### 4. Нормализация капитализации
```cpp
wstring normalizeCapitalization(wstring word) {
    result += toUpperRus(word[0]);     // Первая буква ЗАГЛАВНАЯ
    for (int i = 1; i < word.length(); i++) {
        result += toLowerRus(word[i]); // Остальные строчные
    }
}
```
Преобразует `яНВарь` → `Январь`

### 5. Определение гласных/согласных
```cpp
bool isVowel(wchar_t ch) {
    wstring russianVowels = L"аеёиоуыэюя";
    wstring englishVowels = L"aeiou";
    // Проверяет, есть ли буква в списке гласных
}
```

### 6. Запись в файл
```cpp
ofstream outputFile("output.txt");
outputFile << wstringToString(vowelWords[i]);
```
Конвертирует обратно в Windows-1251 и записывает в файл.

---

## 📚 Типы данных: string vs wstring

### `string` - обычная строка
```cpp
string text = "Hello";
```
- **Тип символа**: `char` (1 байт)
- **Кодировка**: ASCII / Windows-1251
- **Использование**: файлы, английский текст
- **Вывод**: `cout << text;`

### `wstring` - широкая строка
```cpp
wstring text = L"Привет";  // Обратите внимание на L
```
- **Тип символа**: `wchar_t` (2 байта)
- **Кодировка**: Unicode (UTF-16)
- **Использование**: обработка кириллицы и других языков
- **Вывод**: `wcout << text;`

### Сравнительная таблица

| Характеристика | `string` | `wstring` |
|----------------|----------|-----------|
| Префикс | `"текст"` | `L"текст"` |
| Размер символа | 1 байт | 2 байта |
| Вывод | `cout` | `wcout` |
| Файлы | `ifstream` | `wifstream` |
| Кириллица | ❌ Проблемы | ✅ Работает |

### Почему используются оба?

```cpp
// 1. Читаем файл как string (Windows-1251)
string line;
getline(inputFile, line);

// 2. Конвертируем в wstring для обработки
wstring wline = stringToWstring(line);

// 3. Обрабатываем данные
wstring result = normalizeCapitalization(wline);

// 4. Конвертируем обратно для записи
string output = wstringToString(result);
outputFile << output;
```

**Причина**: файлы хранятся в Windows-1251, но для правильной работы с русскими буквами нужен Unicode.

---

## 🔑 Ключевые функции программы

### `stringToWstring()` - конвертация в Unicode
```cpp
wstring stringToWstring(const string& str) {
    MultiByteToWideChar(1251, 0, str.c_str(), ...);
}
```
Преобразует строку из Windows-1251 в UTF-16.

### `wstringToString()` - конвертация обратно
```cpp
string wstringToString(const wstring& wstr) {
    WideCharToMultiByte(1251, 0, wstr.c_str(), ...);
}
```
Преобразует UTF-16 обратно в Windows-1251 для записи в файл.

### `toUpperRus()` / `toLowerRus()` - изменение регистра
```cpp
wchar_t toUpperRus(wchar_t ch) {
    if (ch >= L'а' && ch <= L'я') return wchar_t(ch - 32);
    if (ch >= L'a' && ch <= L'z') return wchar_t(ch - 32);
}
```
Поддерживает как русские, так и английские буквы.

### `isVowel()` - проверка гласной
```cpp
bool isVowel(wchar_t ch) {
    wstring vowels = L"аеёиоуыэюяaeiou";
    // Проверяет наличие буквы в списке
}
```

---

## ⚙️ Настройка кодировки для консоли

```cpp
SetConsoleCP(1251);        // Кодировка ввода
SetConsoleOutputCP(1251);  // Кодировка вывода
setlocale(LC_ALL, "Russian");
```

Эти строки необходимы для правильного отображения русских букв в консоли Windows.

---

## 🐛 Решение проблем

### Проблема: Не находит файл input.txt
**Решение**: Программа показывает путь к рабочей директории. Поместите файл туда или создайте его автоматически (программа сделает это при первом запуске).

### Проблема: В файле "кракозябры" вместо русских букв
**Решение**: Откройте файл в Notepad++ и измените кодировку на Windows-1251:
- Меню → Кодировка → Кодировки → Кириллица → Windows-1251

### Проблема: Консоль сразу закрывается
**Решение**: Запускайте через **Ctrl+F5** в Visual Studio или используйте `system("pause")` в конце программы.

### Проблема: Неправильно определяет гласные/согласные
**Решение**: Проверьте, что файл `input.txt` сохранен в правильной кодировке (ANSI или Windows-1251).

---

## 📝 Требования

- **Компилятор**: Visual Studio 2017 или новее
- **Стандарт C++**: C++11 или выше
- **ОС**: Windows (из-за использования `Windows.h` и функций `MultiByteToWideChar`)
- **Кодировка файлов**: Windows-1251 (ANSI)

---

## 💡 Примеры использования

### Создание своего входного файла

Создайте файл `input.txt` со своими данными:
```
пРИвет мИР СОЛНЦЕ ЛуНА зВЕзда КосмоС
```

Результат в `output.txt`:
```
Слова, начинающиеся с гласных:
Привет
Слова, начинающиеся с согласных:
Мир, Солнце, Луна, Звезда, Космос
```

### Работа с английским текстом

```
hELLo WoRLD ApPLE bANAna ORange
```

Результат:
```
Слова, начинающиеся с гласных:
Apple, Orange
Слова, начинающиеся с согласных:
Hello, World, Banana
```

---

## 📖 Дополнительные материалы

- [Документация по string](https://en.cppreference.com/w/cpp/string/basic_string)
- [Документация по wstring](https://en.cppreference.com/w/cpp/string/basic_string)
- [Работа с файлами в C++](https://en.cppreference.com/w/cpp/io/basic_fstream)
- [Кодировки Windows](https://docs.microsoft.com/ru-ru/windows/win32/intl/code-pages)
