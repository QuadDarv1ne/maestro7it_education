# Методические указания для преподавателя
## Домашнее задание: Анализ образовательной платформы

---

## 📋 Структура занятия по разбору решения

**Время:** 90-120 минут  
**Формат:** Live coding с объяснениями

---

## 🎯 Цели занятия

Студенты должны научиться:
- Работать с реляционными данными (merge, join)
- Проводить группировки и агрегации (groupby, pivot)
- Создавать информативные визуализации
- Делать выводы на основе данных
- Документировать аналитический код

---

## 📝 Разбор по частям

### ЧАСТЬ 1: Импорт и настройка (5 минут)

**Что объяснить:**
- Зачем нужны все три библиотеки:
  - `pandas` — табличные данные
  - `matplotlib` — базовые графики
  - `seaborn` — статистические визуализации
- Настройка стиля графиков **до** начала работы
- `warnings.filterwarnings('ignore')` — почему это OK для учебных проектов

**Распространённые ошибки студентов:**
- Забывают импортировать `numpy`
- Не настраивают `figsize` → графики нечитаемы
- Импортируют библиотеки в середине кода

**Лайфхак:** Покажите, как проверить версии библиотек:
```python
print(pd.__version__)
```

---

### ЧАСТЬ 2: Генерация данных (10 минут)

**Что объяснить:**
- В реальности данные загружаются через `pd.read_csv('file.csv')`
- Структура данных имитирует реальную БД (primary keys, foreign keys)
- Зачем нужен `np.random.seed(42)` — воспроизводимость результатов

**Важные концепции:**
1. **Связи между таблицами:**
   - `students.student_id` ← `enrollments.student_id` (one-to-many)
   - `courses.course_id` ← `enrollments.course_id` (one-to-many)
   - `enrollments.enrollment_id` ← `progress.enrollment_id` (one-to-one)

2. **Realistic data:**
   - Веса в `np.random.choice` — Москва популярнее
   - Пропуски в данных — как в реальности

**Задание для самостоятельной работы:**
- Попросите студентов нарисовать схему связей таблиц на бумаге

---

### ЧАСТЬ 3: Первичное изучение (15 минут)

**Что объяснить:**

#### 3.1 Обязательные проверки при получении данных:
```python
df.head()          # Первые строки
df.info()          # Типы и память
df.describe()      # Статистика
df.shape           # Размер
df.isnull().sum()  # Пропуски
```

#### 3.2 Почему это важно:
- **Типы данных:** `object` vs `datetime64` — разные операции
- **Пропуски:** нельзя вычислять среднее с NaN
- **Размер:** понимание масштаба датасета

**Демонстрация на примере:**
```python
# Плохо: дата как строка
enrollments['enrollment_date'] + 7  # ❌ Ошибка!

# Хорошо: преобразовали в datetime
pd.to_datetime(enrollments['enrollment_date']) + pd.Timedelta(days=7)  # ✓
```

**Типичная ошибка студентов:**
Сразу начинают merge, не изучив данные → получают неожиданные результаты

---

### ЧАСТЬ 4: Объединение таблиц (20 минут)

**Ключевая тема!** Здесь студенты часто ошибаются.

#### 4.1 Последовательность merge:
```
enrollments + progress → df_temp
df_temp + students     → df_temp
df_temp + courses      → df_final
```

**Объяснить:**
- Почему именно в таком порядке (от центральной таблицы)
- Разница `how='left'` vs `how='inner'`:
  - `left` — сохраняем все записи из левой таблицы
  - `inner` — только совпадения
  - `outer` — все из обеих

#### 4.2 Визуализация на доске:
```
enrollments (280 строк)  +  progress (280 строк)
         ↓
    result (280 строк)   ← LEFT JOIN сохраняет все

enrollments (280 строк)  +  students (150 строк)
         ↓
    result (280 строк)   ← Один студент → много курсов
```

#### 4.3 Проверка после merge:
```python
# Потеряли ли мы строки?
print(f"До: {len(enrollments)}, После: {len(df)}")

# Появились ли дубликаты?
print(df.duplicated().sum())
```

**Практическое задание:**
- Попросите студентов предсказать размер после каждого merge
- Спросите: что будет, если использовать `how='inner'`?

---

### ЧАСТЬ 5: Подготовка данных (15 минут)

**Что объяснить:**

#### 5.1 Обработка пропусков:
```python
# Стратегия 1: Удалить
df.dropna()

# Стратегия 2: Заполнить константой
df.fillna(0)

# Стратегия 3: Заполнить статистикой
df.fillna(df.mean())

# Стратегия 4: Умная замена (наш случай)
df.groupby('category')['grade'].transform(lambda x: x.fillna(x.median()))
```

**Почему групповая медиана?**
- У разных категорий разные оценки
- Медиана устойчива к выбросам (vs mean)

#### 5.2 Feature Engineering:
```python
# Категоризация возраста
pd.cut(df['age'], bins=[17, 25, 35, 50], labels=['18-25', '26-35', '36+'])

# Извлечение месяца
pd.to_datetime(df['date']).dt.month

# Расчёт метрик
df['completion_rate'] = df['lessons_done'] / 50 * 100
```

**Покажите на примере:**
```python
# До
age: [22, 28, 35, 41]

# После
age_group: ['18-25', '26-35', '26-35', '36+']
```

**Зачем это нужно?**
- Упрощение анализа
- Лучшая визуализация
- Создание бизнес-метрик

---

### ЧАСТЬ 6: Анализ и визуализация (40 минут)

Это **основная часть**. Разбирайте каждый график подробно.

---

#### 📊 Вопрос 1: Популярность курсов (8 мин)

**Pandas техники:**
```python
# Группировка + агрегация + переименование + сортировка
df.groupby('course_name').agg({
    'enrollment_id': 'count',
    'price': 'first'
}).rename(columns={'enrollment_id': 'enrollments'}).sort_values('enrollments', ascending=False)
```

**Разбор по шагам:**
1. `groupby` — группируем по названию курса
2. `agg` — применяем разные функции к разным колонкам
3. `rename` — понятные названия
4. `sort_values` — сортируем результат

**Matplotlib:**
```python
# Горизонтальный bar chart лучше для длинных названий
ax.barh(y_labels, x_values)
ax.invert_yaxis()  # Самый популярный сверху
```

**Scatter plot + trend line:**
```python
# Линия тренда — показываем корреляцию визуально
z = np.polyfit(x, y, 1)  # Полином 1-й степени (линейная регрессия)
p = np.poly1d(z)
ax.plot(x, p(x), "r--")
```

**Интерпретация:**
- Если линия вниз → цена мешает популярности
- Если горизонтальная → цена не влияет
- Если вверх → дорогие курсы популярнее

---

#### 📊 Вопрос 2: Возрастные группы (8 мин)

**Pandas техники:**
```python
df.groupby('age_group').agg({
    'enrollment_id': 'count',      # Количество
    'completion_rate': 'mean',     # Среднее
    'average_grade': 'mean'
})
```

**Визуализация:**
```python
# 3 графика на одном figure
fig, axes = plt.subplots(1, 3, figsize=(18, 5))

# Обращение к конкретной оси
axes[0].bar(...)
axes[1].bar(...)
axes[2].bar(...)
```

**Покажите:**
- Зачем 3 графика вместо 1: каждый отвечает на свой вопрос
- `axhline` — горизонтальная линия среднего для сравнения
- Палитры цветов — как улучшить читаемость

**Обсуждение с аудиторией:**
"Какая возрастная группа — целевая аудитория? Почему?"

---

#### 📊 Вопрос 3: Прогресс по категориям (8 мин)

**Новые типы графиков:**

1. **Box plot** — показывает распределение:
```python
df.boxplot(column='completion_rate', by='category')
```
- Медиана (линия в центре)
- Квартили (границы ящика)
- Выбросы (точки)

2. **Violin plot** — Box plot + плотность распределения:
```python
sns.violinplot(data=df, x='category', y='grade')
```

**Интерпретация Box plot:**
```
    |        Макс (не выброс)
    |
    ├────┐   75-й перцентиль (Q3)
    │    │
    ├────┤   Медиана (Q2)
    │    │
    ├────┘   25-й перцентиль (Q1)
    |
    |        Мин (не выброс)
    •        Выброс
```

**Когда что использовать:**
- Box plot → сравнение медиан
- Violin plot → понимание распределения

---

#### 📊 Вопрос 4: География (8 мин)

**Pandas техники:**
```python
# Кросс-таблица с процентами
pd.crosstab(df['city'], df['category'], normalize='index') * 100
```

**Объяснить `normalize`:**
- `normalize='index'` → проценты по строкам (сумма = 100% в строке)
- `normalize='columns'` → по столбцам
- `normalize='all'` → от общей суммы

**Stacked bar chart:**
```python
city_courses.plot(kind='bar', stacked=True)
```
- Показывает структуру (100% = все категории)
- Легко сравнивать города

**Легенда:**
```python
plt.legend(bbox_to_anchor=(1.05, 1), loc='upper left')
```
- Выносим легенду за пределы графика (не перекрывает данные)

---

#### 📊 Вопрос 5: Динамика (5 мин)

**Работа с датами:**
```python
df['date'].dt.to_period('M')  # Группировка по месяцам
df['date'].dt.month           # Извлечение месяца
df['date'].dt.year            # Извлечение года
```

**Line plot:**
```python
registrations.plot(kind='line', marker='o')
```
- `marker='o'` → точки на графике (видно каждое значение)
- `linewidth=2` → жирная линия (лучше видно)

**Когда использовать:**
- Временные ряды
- Тренды
- Сезонность

---

#### 📊 Вопрос 6: Завершение курсов (5 мин)

**Pandas техника — lambda функции:**
```python
df.groupby('course').agg({
    'status': lambda x: (x == 'completed').sum() / len(x) * 100
})
```

**Объяснить:**
1. `x == 'completed'` → булев массив [True, False, True...]
2. `.sum()` → считает True как 1
3. `/ len(x) * 100` → процент

**Grouped bar chart:**
```python
x = np.arange(len(data))
ax.bar(x - width/2, data1, width, label='Метрика 1')
ax.bar(x + width/2, data2, width, label='Метрика 2')
```
- Сравнение двух метрик для каждого курса

---

#### 📊 Вопрос 7: Корреляция (6 мин)

**Корреляционная матрица:**
```python
df[['age', 'grade', 'completion', 'price']].corr()
```

**Интерпретация:**
- 1.0 → идеальная положительная корреляция
- 0.0 → нет связи
- -1.0 → идеальная отрицательная корреляция

**Heatmap:**
```python
sns.heatmap(corr, annot=True, cmap='coolwarm', center=0)
```
- `annot=True` → числа на ячейках
- `cmap='coolwarm'` → красный (положительное), синий (отрицательное)
- `center=0` → белый = ноль корреляции

**Практический совет:**
Корреляция ≠ причинность! Объясните на примере.

---

### ЧАСТЬ 7: Выводы и рекомендации (10 минут)

**Что объяснить:**

#### 7.1 Разница между аналитикой и аналитическим мышлением:

**Плохо:**
> "Средняя оценка = 4.2"

**Хорошо:**
> "Средняя оценка 4.2 из 5.0, что указывает на высокое качество контента. Однако 15% студентов бросают курсы — рекомендуем внедрить систему поддержки."

#### 7.2 Структура выводов:
1. Факт (что увидели в данных)
2. Интерпретация (что это значит)
3. Рекомендация (что делать)

**Примеры:**

**Факт:** Курсы по Data Science показывают лучший completion rate (65%)
**Интерпретация:** Эта категория наиболее востребована и качественна
**Рекомендация:** Расширить портфолио курсов в этой категории

---

## 🎓 Критерии оценки домашнего задания

### Отлично (5):
- ✅ Все таблицы корректно объединены
- ✅ Использованы groupby, merge, разные типы визуализаций
- ✅ Код чистый, с комментариями
- ✅ Сделаны осмысленные выводы
- ✅ Графики информативны и красивы

### Хорошо (4):
- ✅ Таблицы объединены с небольшими недочётами
- ✅ Использованы основные методы pandas
- ✅ Есть базовые комментарии
- ✅ Сделаны выводы
- ⚠️ Графики базовые, не все подписаны

### Удовлетворительно (3):
- ⚠️ Объединение таблиц с ошибками
- ⚠️ Использованы только простые операции
- ❌ Минимум комментариев
- ⚠️ Выводы поверхностные
- ❌ Графики без подписей

### Неудовлетворительно (2):
- ❌ Не все таблицы объединены
- ❌ Ошибки в коде
- ❌ Нет комментариев
- ❌ Нет выводов

---

## 🔧 Частые ошибки студентов и как их избежать

### 1. Merge теряет строки
**Проблема:**
```python
df = enrollments.merge(students, on='student_id')  # Потеряли 20 строк!
```

**Решение:**
```python
df = enrollments.merge(students, on='student_id', how='left')
print(f"Исходных строк: {len(enrollments)}, Получилось: {len(df)}")
```

### 2. Некорректная группировка
**Проблема:**
```python
df.groupby('category').mean()  # Усредняет всё, даже ID!
```

**Решение:**
```python
df.groupby('category').agg({
    'grade': 'mean',
    'completion': 'mean'
})
```

### 3. Нечитаемые графики
**Проблема:**
```python
plt.plot(data)  # Без подписей, без заголовка
```

**Решение:**
```python
plt.plot(data)
plt.xlabel('Описание X')
plt.ylabel('Описание Y')
plt.title('Понятный заголовок')
plt.legend()
```

### 4. Игнорирование NaN
**Проблема:**
```python
df['grade'].mean()  # NaN портит результат
```

**Решение:**
```python
df['grade'].fillna(df['grade'].median()).mean()
```

### 5. Перегруженные графики
**Проблема:**
- Слишком много данных на одном графике
- Нечитаемые подписи

**Решение:**
- Топ-10 вместо всех значений
- Поворот подписей: `plt.xticks(rotation=45)`
- Увеличение размера: `figsize=(14, 6)`

---

## 💡 Дополнительные задания для продвинутых

Если студент выполнил основное задание:

1. **Добавить временной анализ:**
   - Как меняется completion rate по месяцам?
   - Есть ли сезонность в регистрациях?

2. **Когортный анализ:**
   - Сравнить студентов, зарегистрировавшихся в разные месяцы

3. **Предиктивная модель:**
   - Попробовать предсказать, бросит ли студент курс

4. **Интерактивный dashboard:**
   - Использовать `plotly` для интерактивных графиков

5. **A/B тестирование:**
   - Добавить поле "экспериментальная группа"
   - Сравнить метрики между группами

---

## 📚 Полезные ресурсы для студентов

**Pandas:**
- [Официальная документация](https://pandas.pydata.org/docs/)
- [10 minutes to pandas](https://pandas.pydata.org/docs/user_guide/10min.html)

**Matplotlib:**
- [Pyplot tutorial](https://matplotlib.org/stable/tutorials/pyplot.html)
- [Gallery примеров](https://matplotlib.org/stable/gallery/index.html)

**Seaborn:**
- [Tutorial](https://seaborn.pydata.org/tutorial.html)
- [Example gallery](https://seaborn.pydata.org/examples/index.html)

**Общее:**
- [Kaggle Learn](https://www.kaggle.com/learn) — бесплатные курсы
- [Python Data Science Handbook](https://jakevdp.github.io/PythonDataScienceHandbook/)

---

## ⏱️ Тайминг разбора занятия

| Часть | Время | Активность |
|-------|-------|-----------|
| Введение | 5 мин | Обзор задачи, структуры данных |
| Импорт и настройка | 5 мин | Объяснение библиотек |
| Генерация данных | 10 мин | Обсуждение структуры БД |
| Изучение данных | 15 мин | `.info()`, `.describe()`, `.isnull()` |
| **Merge таблиц** | 20 мин | **Ключевая тема!** |
| Подготовка данных | 15 мин | NaN, feature engineering |
| **Визуализации** | 40 мин | **Разбор каждого графика** |
| Выводы | 10 мин | Аналитическое мышление |
| Q&A | 10 мин | Вопросы студентов |

**Итого:** 120 минут (2 академических часа)

---

## ✅ Чек-лист перед занятием

- [ ] Убедиться, что у всех студентов установлены библиотеки
- [ ] Подготовить Jupyter Notebook с кодом
- [ ] Подготовить презентацию со схемой данных
- [ ] Приготовить примеры распространённых ошибок
- [ ] Подготовить дополнительные задания для быстрых студентов
- [ ] Настроить screen sharing для демонстрации кода
- [ ] Подготовить ссылки на полезные ресурсы

---

## 🎯 Главное, что студенты должны вынести

1. **Pandas — это SQL для Python**: merge = JOIN, groupby = GROUP BY
2. **Visualizations рассказывают истории**: каждый график отвечает на вопрос
3. **Data cleaning — 80% работы**: изучение, очистка, подготовка
4. **От данных к решениям**: анализ → выводы → рекомендации
5. **Документируйте код**: комментарии помогают вам же через месяц

---

**Удачи на занятии! 🚀**