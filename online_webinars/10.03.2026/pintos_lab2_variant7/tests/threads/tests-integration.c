/* Дополнительный файл для интеграции теста в Pintos
 * Добавьте следующие строки в tests/threads/tests.c:
 * 
 * extern void test_new_alg(void);
 * 
 * И в массив tests[] добавьте:
 * {"new-alg", test_new_alg},
 */

#include "tests/threads/tests.h"
#include <stdio.h>

/* Описание теста test-new-alg для варианта 7
 * 
 * Цель теста:
 * - Демонстрация приоритетного планирования процессов
 * - Проверка правильности порядка выполнения по приоритету
 * - Тестирование Round Robin для процессов с одинаковым приоритетом
 * 
 * Ожидаемое поведение:
 * 1. Proc0 (приоритет 27) выполняется первым, завершается за 3 тика
 * 2. Proc3 (приоритет 18) выполняется вторым, завершается за 1 тик
 * 3. Proc2 (приоритет 8) выполняется третьим, завершается за 20 тиков
 * 4. Proc1 (приоритет 7) выполняется последним, завершается за 8 тиков
 * 
 * Общее время: 32 тика (3 + 8 + 20 + 1)
 */

void
run_test_new_alg(void)
{
  printf("Запуск теста new-alg...\n");
  test_new_alg();
}
