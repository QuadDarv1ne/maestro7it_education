# Лабораторная работа: Использование технологий вычислений на GPU

## Выполнил: Студент

**Версия:** 1.5.0 (последнее обновление: 2026-03-20)

[![CI/CD](https://github.com/your-repo/gpu-lab/actions/workflows/ci.yml/badge.svg)](https://github.com/your-repo/gpu-lab/actions)

---

## 📊 Результаты производительности

| Тест | CPU | GPU Kernel | Ускорение |
|------|-----|------------|-----------|
| SHA-256 (10000 хэшей) | 7 мс | 0.116 мс | **60.5x** |
| Решето Эратосфена (N=10⁶) | 2 мс | 191 мс* | 0.01x* |

> *GPU overhead значителен для малых объёмов данных. Ускорение проявляется при больших N (>10⁷).

**Тестовая система:**
- GPU: NVIDIA GeForce RTX 4060 Laptop GPU (24 SM, 8GB)
- CPU: GCC 15.2.0, Windows
- OpenCL: 3.0 CUDA

---

## 📋 Быстрый старт

### 🚀 Автоматическая сборка (рекомендуется)

**Linux/macOS:**
```bash
chmod +x scripts/build_and_test.sh
./scripts/build_and_test.sh          # Сборка + тесты
./scripts/build_and_test.sh -c -b    # Очистка + сборка + бенчмарки
./scripts/build_and_test.sh --debug  # Debug сборка
```

**Windows:**
```cmd
scripts\build_and_test.bat           # Сборка + тесты
scripts\build_and_test.bat -c -b     # Очистка + сборка + бенчмарки
scripts\build_and_test.bat --debug   # Debug сборка
```

### 🔧 Ручная сборка через CMake

```bash
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .

# Запуск тестов
ctest --verbose

# Запуск программ
./sieve 10000000 256
./hash 100000 64 256
```

### 🎯 CMake Presets (рекомендуется)

**CMake 3.19+:**
```bash
# Конфигурация и сборка
cmake --preset release
cmake --build --preset release

# Запуск тестов
ctest --preset release

# Debug сборка
cmake --preset debug
cmake --build --preset debug

# Сборка с AddressSanitizer
cmake --preset asan
cmake --build --preset asan

# Сборка с покрытием кода
cmake --preset coverage
cmake --build --preset coverage
```

**Доступные пресеты:**
- `release` — Release сборка с оптимизациями
- `debug` — Debug сборка с отладочной информацией
- `asan` — Debug + AddressSanitizer для поиска ошибок памяти
- `coverage` — Debug + покрытие кода
- `windows-release` — Windows Release (MSVC)

### 🧹 Очистка от мусора

**PowerShell (Windows):**
```powershell
./scripts/clean.ps1          # Очистка
./scripts/clean.ps1 -DryRun  # Просмотр без удаления
```

**Bash (Linux/macOS):**
```bash
./scripts/clean.sh           # Очистка
./scripts/clean.sh --dry-run # Просмотр без удаления
```

**CMake цели:**
```bash
cmake --build build --target clean_all      # Очистка build
cmake --build build --target clean_project  # Полная очистка
```

**Что сохраняется:** Все `.exe` файлы  
**Что удаляется:** `.o`, `.obj`, `.a`, `.lib`, `.dll`, `.pyc`, `CMakeFiles/`, `Testing/`, `__pycache__/`

## Структура проекта

```
lab_opencl/
├── include/                  # Общие заголовочные файлы OpenCL
│   └── cl_common.h          # Общие утилиты и функции
├── src/                      # Общие исходные файлы
│   └── cl_utils.c           # Реализация OpenCL утилит
├── sieve/                    # Задача 1: Решето Эратосфена
│   ├── sieve.c              # Основной код (CPU + GPU)
│   ├── sieve.cl             # OpenCL kernels
│   ├── sieve_cpu.c          # CPU версия для тестов
│   └── Makefile             # Сборка
├── hashing/                  # Задача 2: Параллельное хэширование
│   ├── hash.c               # Основной код (CPU + GPU)
│   ├── hash.cl              # OpenCL kernels (SHA-256)
│   ├── sha256.c             # CPU реализация SHA-256
│   ├── sha256.h             # Заголовок SHA-256
│   └── Makefile             # Сборка
├── tests/                    # Unit-тесты и интеграционные тесты
│   ├── test_sha256.c        # Unit-тесты SHA-256
│   ├── test_sieve.c         # Unit-тесты решета
│   ├── test_hash_correctness.c    # CPU vs GPU (hash)
│   ├── test_sieve_correctness.c   # CPU vs GPU (sieve)
│   ├── test_common.h        # Инфраструктура тестирования
│   └── Makefile
├── scripts/                  # Скрипты автоматизации
│   ├── build_and_test.sh    # Автосборка и тесты (Linux/macOS)
│   ├── build_and_test.bat   # Автосборка и тесты (Windows)
│   ├── benchmark.sh         # Бенчмарки
│   ├── setup_linux.sh       # Установка зависимостей
│   └── clean.py             # Очистка артефактов
├── .github/workflows/        # CI/CD конфигурация
│   └── ci.yml               # GitHub Actions workflow
├── CMakeLists.txt            # Кроссплатформенная сборка
├── CHANGELOG.md              # История изменений
├── Doxyfile                  # Конфигурация Doxygen
├── Dockerfile                # Docker образ для сборки
└── README.md                 # Этот файл
```

## Задачи

1. **Решето Эратосфена** — нахождение всех простых чисел до N
2. **Параллельное хэширование** — вычисление SHA-256 хэшей для множества данных

---

## Требования

### Обязательные
- Компилятор C (gcc или clang)
- OpenCL runtime (драйверы GPU с поддержкой OpenCL)
- Make (опционально)

### Установка OpenCL

**Ubuntu/Debian:**
```bash
# Для NVIDIA GPU
sudo apt install nvidia-cuda-toolkit ocl-icd-opencl-dev

# Для AMD GPU
sudo apt install mesa-opencl-icd ocl-icd-opencl-dev

# Для Intel GPU/CPU
sudo apt install intel-opencl-icd ocl-icd-opencl-dev
```

**macOS:**
OpenCL уже встроен в систему (framework OpenCL).

**Windows:**
Установите драйверы GPU (NVIDIA/AMD/Intel), они включают OpenCL runtime.

---

## Сборка и запуск

### Задача 1: Решето Эратосфена

```bash
cd sieve

# Сборка
make

# Запуск с параметрами по умолчанию (N=10^7)
./sieve

# Запуск с указанием N и размера work-group
./sieve 1000000 256

# Тесты
make test

# Бенчмарк с разными размерами work-group
make benchmark
```

**Параметры запуска:**
```
./sieve [N] [local_size]

  N           - верхняя граница поиска простых чисел (по умолчанию 10000000)
  local_size  - размер work-group (по умолчанию 256)
```

### Задача 2: Параллельное хэширование

```bash
cd hashing

# Сборка
make

# Запуск с параметрами по умолчанию (100000 хэшей)
./hash

# Запуск с указанием параметров
./hash 100000 64 256

# Тесты
make test

# Бенчмарк масштабируемости
make benchmark

# Бенчмарк размеров work-group
make benchmark_wg
```

**Параметры запуска:**
```
./hash [num_hashes] [data_len] [local_size]

  num_hashes  - количество хэшей для вычисления (по умолчанию 100000)
  data_len    - максимальная длина данных в байтах (по умолчанию 64)
  local_size  - размер work-group (по умолчанию 256)
```

---

## Алгоритмы

### 1. Решето Эратосфена

**CPU версия:**
Классический последовательный алгоритм:
1. Инициализация массива (все числа простые)
2. Для каждого простого p от 2 до √N:
   - Отметить все кратные p как составные (начиная с p²)
3. Подсчёт оставшихся простых чисел

**GPU версия:**
Параллельная реализация на OpenCL:
- Kernel `init_array`: инициализация массива на GPU
- Kernel `sieve_mark_multiples`: каждый поток отмечает кратные текущего простого
- Kernel `count_primes`: параллельный подсчёт с использованием атомиков

**Оптимизации:**
- Распределение работы между потоками (каждый поток обрабатывает часть кратных)
- Коалесцентный доступ к памяти
- Использование атомарных операций для подсчёта

### 2. Параллельное хэширование (SHA-256)

**CPU версия:**
Стандартная реализация SHA-256:
1. Padding сообщения до длины, кратной 512 битам
2. Разбиение на блоки по 512 бит
3. Для каждого блока: расширение до 64 слов и 64 раунда сжатия

**GPU версия:**
- Каждый поток вычисляет хэш для своего блока данных
- Все хэши вычисляются параллельно
- Поддержка сообщений разной длины

**Дополнительные хэши:**
- DJB2 — простой быстрый хэш
- FNV-1a — лучший для хэш-таблиц
- MurmurHash — быстрый с хорошим распределением

---

## Результаты тестирования

### Решето Эратосфена (N = 10^7)

| Параметр | CPU | GPU |
|----------|-----|-----|
| Время выполнения | ~XXX мс | ~XX мс |
| Ускорение | 1x | ~XXx |
| Найдено простых | 664579 | 664579 |

*Результаты зависят от конкретного GPU.*

### Параллельное хэширование (100000 хэшей)

| Параметр | CPU | GPU |
|----------|-----|-----|
| Время выполнения | ~XXX мс | ~XX мс |
| Ускорение | 1x | ~XXx |
| Скорость | XX хэшей/с | XX хэшей/с |

---

## Оптимизации

### Выбор размера work-group

Размер work-group (local_size) влияет на производительность:
- **Маленький (32-64)**: больше overhead, меньше параллелизм внутри группы
- **Средний (128-256)**: оптимально для большинства задач
- **Большой (512-1024)**: может быть ограничен hardware

Рекомендуется тестировать разные размеры для конкретного GPU.

### Рекомендации

1. **Для решета Эратосфена:**
   - Используйте local_size = 256 для оптимального баланса
   - Для больших N (>10^8) может потребоваться больше памяти

2. **Для хэширования:**
   - SHA-256: local_size = 256-512
   - Простые хэши (DJB2, FNV): можно использовать больший local_size

---

## Переносимость

Код протестирован на:
- **Linux** (Ubuntu 20.04+) с NVIDIA/AMD/Intel OpenCL
- **macOS** (встроенный OpenCL framework)
- **Windows** (с установленными драйверами GPU)

Для компиляции на macOS:
```bash
make macos
```

---

## Возможные ошибки

### "GPU не найден"
- Проверьте установку драйверов GPU
- Убедитесь, что OpenCL runtime установлен
- Программа автоматически переключится на CPU

### "Ошибка компиляции kernel"
- Проверьте версию OpenCL (требуется 1.2+)
- Убедитесь, что файл .cl находится в той же директории

### "Не удалось выделить память"
- Уменьшите параметр N или num_hashes
- Проверьте доступную память GPU

---

## 🆕 Что нового в версии 1.3.0

### Улучшения сборки
- **CMake 3.16+** — обновлённая версия CMake с новыми возможностями
- **Расширенные опции компиляции** — дополнительные предупреждения для gcc/clang
- **Покрытие кода** — опция `ENABLE_COVERAGE` для анализа тестирования
- **Генерация документации** — цель `docs` для Doxygen (при наличии Doxygen)
- **Очистка сборки** — цель `clean_all` для полной очистки артефактов

### Скрипты автоматизации
- **build_and_test.sh/bat** — кроссплатформенные скрипты для сборки и тестов
- **Автоматическое определение ядер** — параллельная сборка с оптимальным числом потоков
- **Цветной вывод** — наглядное отображение статуса операций

### Улучшения обработки ошибок
- **cl_get_error_description()** — расширенное описание ошибок OpenCL
- **Дополнительные ошибки** — поддержка CL_INVALID_MIP_LEVEL, CL_INVALID_GL_OBJECT

### Обновлённая документация
- **Актуализированный README** — быстрый старт с автосборкой
- **Структура проекта** — полное описание всех файлов
- **Примеры запуска** — подробные инструкции для всех платформ

---

## 🆕 Что нового в версии 1.1.0

### Общие модули
- **`include/cl_common.h`** — общие утилиты OpenCL (макросы, структуры, функции)
- **`src/cl_utils.c`** — реализация функций для работы с контекстом, устройствами, файлами
- Устранено дублирование кода между `hash.c` и `sieve.c`

### CMake сборка
- **`CMakeLists.txt`** — кроссплатформенная система сборки
- Опции: `BUILD_TESTS`, `BUILD_BENCHMARKS`, `ENABLE_PROFILING`, `USE_STATIC_ANALYSIS`
- Интеграция с `CTest` для автоматических тестов

### Интеграционные тесты
- **`test_hash_correctness.c`** — сравнение результатов CPU vs GPU для хэширования
- **`test_sieve_correctness.c`** — сравнение результатов CPU vs GPU для решета
- Тесты производительности с различными work-group sizes

### CI/CD
- **GitHub Actions workflow** — автоматическая сборка и тесты
- Платформы: Ubuntu 22.04, Windows 2022
- Статический анализ: clang-tidy, cppcheck
- Генерация документации: Doxygen

### Оптимизации kernel'ов
- **`sha256_hash_optimized()`** — оптимизированная версия SHA-256 kernel
- Использование локальной памяти для констант K (256 байт на work-group)
- Loop unrolling для 64 раундов сжатия
- Потенциальное ускорение: 1.5-2x по сравнению с оригинальной версией

### Улучшения обработки ошибок
- Макросы: `SAFE_FREE()`, `CHECK_CL_ERROR_RET()`, `CHECK_ALLOC_RET()`
- Проверка всех вызовов `malloc()`/`calloc()`
- Корректное освобождение ресурсов при ошибках (метка `cleanup:`)
- Инициализация переменных NULL для безопасного освобождения

### Документация
- **`CHANGELOG.md`** — история изменений проекта
- Обновлённый `README.md` с быстрым стартом

---

## 📊 Сравнение версий

| Функция | Версия 1.0 | Версия 1.1 | Версия 1.3 | Версия 1.5 |
|---------|------------|------------|------------|------------|
| Сборка | Makefile | Makefile + CMake | CMake 3.16+ | CMake 3.16+ + LTO |
| Тесты | 2 unit-теста | 6 тестов | 6 тестов + coverage | 6 тестов (100% pass) |
| CI/CD | ❌ | ✅ GitHub Actions | ✅ GitHub Actions | ✅ GitHub Actions |
| Общие модули | ❌ | ✅ include/ + src/ | ✅ Улучшенные | ✅ Полные |
| Оптимизированные kernel'ы | ❌ | ✅ sha256_hash_optimized | ✅ | ✅ + бенчмарки |
| Обработка ошибок | Базовая | Расширенная (macro) | ✅ + cl_get_error_description | ✅ + валидация |
| Скрипты автоматизации | ❌ | ❌ | ✅ build_and_test | ✅ + clean |
| Документация | ❌ | ✅ CHANGELOG | ✅ Doxygen + README | ✅ + Известные проблемы |
| Docker | ❌ | ❌ | ✅ Dockerfile | ✅ |
| Покрытие кода | ❌ | ❌ | ✅ Опционально | ✅ Опционально |
| Предупреждения | ⚠️ | ⚠️ | ⚠️ | ✅ Исправлены |

---

## Теоретические основы

### Решето Эратосфена
Алгоритм находит все простые числа до N за время O(n log log n).

Количество простых чисел до N приближённо равно N / ln(N) (теорема о распределении простых чисел).

### SHA-256
Криптографическая хэш-функция:
- Выход: 256 бит (32 байта)
- Размер блока: 512 бит
- Количество раундов: 64
- Стойкость к коллизиям: ~2^128 операций

---

## Выводы

1. GPU обеспечивает значительное ускорение для задач с массовым параллелизмом
2. Оптимальный размер work-group зависит от конкретного GPU и задачи
3. OpenCL позволяет создавать переносимый код для разных платформ
4. Параллельные алгоритмы требуют особого подхода к работе с памятью

---

## ⚠️ Известные проблемы и ограничения

### Производительность
- **GPU overhead:** Для малых объёмов данных (<10000 элементов) overhead на передачу данных превышает время вычислений
- **Решето Эратосфена:** На GPU работает медленнее CPU для N<10⁷ из-за накладных расходов на передачу данных
- **Оптимальный batch size:** Для достижения ускорения рекомендуется обрабатывать от 10000 элементов за раз

### Сборка
- **Windows:** Требуется `shlwapi.lib` для функции `PathRemoveFileSpecA`
- **MinGW:** Может потребоваться явное указание пути к OpenCL (`-LC:/msys64/ucrt64/lib`)
- **LTO:** Link Time Optimization может увеличить время сборки на 20-30%

### Тестирование
- **CI/CD:** В GitHub Actions тесты выполняются только на CPU (отсутствует GPU в runner)
- **AddressSanitizer:** Не рекомендуется использовать с Release сборкой

### Совместимость
- **OpenCL 3.0:** Требуется минимум OpenCL 1.2, рекомендуется 3.0
- **Драйверы:** Убедитесь, что драйверы GPU поддерживают OpenCL (NVIDIA CUDA, AMD ROCm, Intel OpenCL)

---

## Использованные источники

1. OpenCL Specification: https://www.khronos.org/opencl/
2. FIPS 180-4: Secure Hash Standard (SHA-256)
3. Sieve of Eratosthenes: https://en.wikipedia.org/wiki/Sieve_of_Eratosthenes
4. Docker Documentation: https://docs.docker.com/
5. CMake Documentation: https://cmake.org/cmake/help/latest/

---

## 📞 Контакты

**Вопросы и предложения:** Создайте issue в репозитории проекта

---

## 📝 Лицензия

Проект распространяется в образовательных целях.
