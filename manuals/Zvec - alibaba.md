# ZVec - Встраиваемая векторная база данных от Alibaba

`ZVec` — это легковесная и сверхбыстрая встраиваемая векторная база данных с открытым исходным кодом, разработанная `Alibaba`

Она создана на базе `Proxima` — проверенного движка векторного поиска от `Alibaba`, что обеспечивает производственное качество, низкую задержку и масштабируемость.

## ✨ Основные возможности

- **Высокая скорость**: поиск среди миллиардов векторов за миллисекунды
- **Простота использования**: установка и начало работы за секунды без настройки серверов и конфигурации
- **Поддержка плотных и разреженных векторов**: работа с разными типами эмбеддингов и мультивекторные запросы
- **Гибридный поиск**: комбинация семантического сходства и структурированных фильтров
- **Кроссплатформенность**: работает как встраиваемая библиотека на серверах, в ноутбуках, CLI-инструментах и даже на периферийных устройствах

## 📦 Установка

### Python (3.10–3.12)

```bash
pip install zvec
```

### Node.js

```bash
npm install @zvec/zvec
```

**Поддерживаемые платформы**: Linux (x86_64, ARM64), macOS (ARM64)

## ⚡ Пример использования (Python)

```python
import zvec

# Определение схемы коллекции
schema = zvec.CollectionSchema(
    name="example",
    vectors=zvec.VectorSchema("embedding", zvec.DataType.VECTOR_FP32, 4),
)

# Создание и открытие коллекции
collection = zvec.create_and_open(path="./zvec_example", schema=schema)

# Вставка документов
collection.insert([
    zvec.Doc(id="doc_1", vectors={"embedding": [0.1, 0.2, 0.3, 0.4]}),
    zvec.Doc(id="doc_2", vectors={"embedding": [0.2, 0.3, 0.4, 0.1]}),
])

# Поиск по векторному сходству
results = collection.query(
    zvec.VectorQuery("embedding", vector=[0.4, 0.3, 0.3, 0.1]),
    topk=10
)

print(results)
```

## 📈 Производительность

`ZVec` обеспечивает исключительную скорость и эффективность, подходящую для самых требовательных рабочих нагрузок. *Подробные бенчмарки доступны в официальной документации.*

## 🤝 Сообщество и вклад

- Проект имеет более 2.7k звёзд на `GitHub`
- Приветствует вклад сообщества
- Возможность присоединиться через `DingTalk`, `WeChat`, `Discord` или следить в `X (Twitter)`
- Доступно руководство для контрибьюторов

Репозиторий написан в основном на `C++ (81.5%)`, а обёртки доступны для `Python` и `Node.js`

## 🌐 Ссылки

- [Официальный репозиторий на GitHub](https://github.com/alibaba/zvec)
- [Документация](https://github.com/alibaba/zvec/blob/main/docs/README.md)

## 🏗 Архитектура

**ZVec построен на основе Proxima, что обеспечивает:**

- Высокую надежность и стабильность
- Производительность уровня `production`
- Низкую задержку при поиске
- Масштабируемость для больших объемов данных

## 🔧 Дополнительные возможности

- Поддержка различных типов индексов
- Гибкая система фильтрации
- Поддержка различных метрик сходства
- Интеграция с популярными фреймворками машинного обучения

## 📋 Сравнение с другими решениями

| Характеристика | ZVec | Chroma | Pinecone |
|---|---|---|---|
| Тип | Встраиваемая | Встраиваемая | Облачная |
| Язык | C++, Python, Node.js | Python | API |
| Размер | Легковесная | Средняя | - |
| Задержка | Низкая | Средняя | Низкая |

*Обратите внимание, что точные характеристики могут отличаться в зависимости от конфигурации и объема данных.*
