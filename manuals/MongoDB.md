# 📦 Полный мануал по MongoDB: Установка, Настройка и Использование

## 📋 Содержание

1. [Введение в MongoDB](#введение-в-mongodb)
2. [Системные требования](#системные-требования)
3. [Установка MongoDB](#установка-mongodb)
4. [Базовая настройка](#базовая-настройка)
5. [Архитектура MongoDB](#архитектура-mongodb)
6. [Конфигурация MongoDB](#конфигурация-mongodb)
7. [Управление пользователями](#управление-пользователями)
8. [Основные команды MongoDB](#основные-команды-mongodb)
9. [Работа с документами](#работа-с-документами)
10. [Индексы и оптимизация](#индексы-и-оптимизация)
11. [Практические примеры](#практические-примеры)
12. [Лучшие практики](#лучшие-практики)

## Введение в MongoDB

**MongoDB** — это кросс-платформенная документоориентированная система управления базами данных с открытым исходным кодом. 

Она классифицируется как NoSQL база данных, которая использует формат, подобный JSON, для хранения и запроса данных.

**Основные особенности MongoDB:**

- Документоориентированное хранилище
- Горизонтальное масштабирование (шардинг)
- Репликация и отказоустойчивость
- Гибкая схема данных
- Поддержка индексов
- Агрегационная платформа
- Поддержка Geospatial queries

## Системные требования

### Минимальные требования:

- **Операционная система:** Windows 7+, Linux (64-bit), macOS 10.12+
- **Процессор:** 64-бит (рекомендуется x86_64)
- **Память:** 512 МБ RAM (рекомендуется 2 ГБ+)
- **Место на диске:** 1 ГБ свободного места
- **Поддержка SSE 4.2:** для MongoDB 4.2+

### Рекомендуемые требования:

- **Операционная система:** Ubuntu 18.04+, CentOS 7+, Windows Server 2016+, macOS 11+
- **Процессор:** Multi-core, 64-bit
- **Память:** 8 ГБ+ RAM
- **Место на диске:** 10 ГБ+ свободного места
- **SSD диск:** для лучшей производительности
- **Свободная память:** 4 ГБ+ для выполнения запросов

### Поддерживаемые версии MongoDB:

- **MongoDB 7.0** (актуальная стабильная)
- **MongoDB 6.0** (рекомендуемая LTS)
- **MongoDB 5.0** (поддерживается)
- **MongoDB 4.4** (EOL: 2024)

## Установка MongoDB

### Метод №1: Установка на Ubuntu/Debian (рекомендуется)

#### Установка из официального репозитория:

```bash
# Импортируйте публичный GPG ключ
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# Создайте файл списка пакетов
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# Обновите список пакетов
sudo apt-get update

# Установите MongoDB
sudo apt-get install -y mongodb-org

# Запустите MongoDB
sudo systemctl start mongod
sudo systemctl enable mongod
```

### Метод №2: Установка на CentOS/RHEL

#### Установка из официального репозитория:

```bash
# Создайте файл репозитория
sudo touch /etc/yum.repos.d/mongodb-org-7.0.repo

# Добавьте конфигурацию репозитория
sudo bash -c 'cat <<EOF > /etc/yum.repos.d/mongodb-org-7.0.repo
[mongodb-org-7.0]
name=MongoDB Repository
baseurl=https://repo.mongodb.org/yum/redhat/\$releasever/mongodb-org/7.0/x86_64/
gpgcheck=1
enabled=1
gpgkey=https://www.mongodb.org/static/pgp/server-7.0.asc
EOF'

# Установите MongoDB
sudo yum install -y mongodb-org

# Запустите MongoDB
sudo systemctl start mongod
sudo systemctl enable mongod
```

### Метод №3: Установка на Windows

#### Установка с помощью MSI установщика:

1. **Скачайте установщик:**
   - Перейдите на https://www.mongodb.com/try/download/community
   - Скачайте последнюю версию Community Server

2. **Запустите установку:**
   - Запустите установочный файл как администратор
   - Следуйте инструкциям мастера установки
   - Выберите "Complete" установку

3. **Настройте сервис:**
   - MongoDB будет установлен как Windows Service
   - Сервис автоматически запустится после установки

### Метод №4: Установка с помощью Docker

#### Запуск MongoDB в Docker:

```bash
# Запустите MongoDB с минимальной конфигурацией
docker run -d --name mongodb -p 27017:27017 mongo:7.0

# Или с аутентификацией и монтированием тома
docker run -d --name mongodb -p 27017:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=admin \
  -e MONGO_INITDB_ROOT_PASSWORD=password \
  -v mongodb_data:/data/db \
  mongo:7.0

# Подключитесь к контейнеру
docker exec -it mongodb mongosh
```

## Базовая настройка

### Настройка конфигурационного файла:

- **Ubuntu/Debian:** `/etc/mongod.conf`
- **CentOS/RHEL:** `/etc/mongod.conf`
- **Windows:** `C:\\Program Files\\MongoDB\\Server\\7.0\\bin\\mongod.cfg`

**Пример минимальной настройки:**

```yaml
# /etc/mongod.conf
storage:
  dbPath: /var/lib/mongo
  journal:
    enabled: true

systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

net:
  port: 27017
  bindIp: 127.0.0.1

processManagement:
  fork: true
  pidFilePath: /var/run/mongodb/mongod.pid
  timeZoneInfo: /usr/share/zoneinfo
```

### Проверка установки:

```bash
# Подключение через mongo shell (устаревший, но все еще работает)
mongo

# Подключение через mongosh (новый shell)
mongosh

# Проверка состояния сервиса
sudo systemctl status mongod
```

## Архитектура MongoDB

### Основные компоненты:

- **MongoDB Server** - основной процесс базы данных
- **Collections** - группы документов (аналог таблиц в SQL)
- **Documents** - единицы хранения данных (аналог строк в SQL)
- **Databases** - контейнеры для коллекций
- **Indexes** - ускоряют поиск документов
- **Replica Sets** - обеспечивает отказоустойчивость
- **Shards** - обеспечивают масштабируемость

### Структура данных:

```
MongoDB Instance
├── Database 1
│   ├── Collection 1
│   │   ├── Document 1 { "_id": ObjectId("..."), "field": "value" }
│   │   ├── Document 2 { "_id": ObjectId("..."), "field": "value" }
│   │   └── Index 1
│   └── Collection 2
├── Database 2
│   └── Collections...
└── System databases (admin, local, config)
```

## Конфигурация MongoDB

### Основные параметры mongod.conf:

#### Storage настройки:

```yaml
storage:
  dbPath: /var/lib/mongo
  journal:
    enabled: true
  engine: wiredTiger
  wiredTiger:
    engineConfig:
      cacheSizeGB: 1
    collectionConfig:
      blockCompressor: snappy
```

#### Сетевые настройки:

```yaml
net:
  port: 27017
  bindIp: 127.0.0.1,192.168.1.100
  maxIncomingConnections: 65536
  wireObjectCheck: true
```

#### Логирование:

```yaml
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log
  verbosity: 0
  quiet: false
```

#### Управление процессом:

```yaml
processManagement:
  fork: true
  pidFilePath: /var/run/mongodb/mongod.pid
```

### Включение аутентификации:

```yaml
# В конфигурации добавьте:
security:
  authorization: enabled
```

**После включения аутентификации нужно создать первого пользователя:**

```javascript
// Подключитесь без аутентификации и выполните:
use admin
db.createUser({
  user: "admin",
  pwd: "password",
  roles: ["userAdminAnyDatabase", "readWriteAnyDatabase"]
})
```

## Управление пользователями

### Создание пользователя:

```javascript
// Подключитесь к MongoDB и выполните:
use admin

// Создание пользователя с ролью
db.createUser({
  user: "myUser",
  pwd: "password",
  roles: [
    { role: "readWrite", db: "myDatabase" },
    { role: "read", db: "otherDatabase" }
  ]
})

// Создание пользователя с привилегиями
db.createUser({
  user: "dbAdmin",
  pwd: "password",
  roles: ["dbAdmin", "readWrite"]
})
```

### Назначение ролей:

```javascript
// Добавление роли пользователю
db.grantRolesToUser("username", [{role: "readWrite", db: "databaseName"}])

// Удаление роли у пользователя
db.revokeRolesFromUser("username", [{role: "readWrite", db: "databaseName"}])

// Просмотр ролей пользователя
db.getUser("username")

// Просмотр всех пользователей
db.getUsers()
```

### Основные роли MongoDB:

- **read** - доступ на чтение
- **readWrite** - доступ на чтение/запись
- **dbAdmin** - администрирование базы данных
- **userAdmin** - управление пользователями
- **clusterAdmin** - администрирование кластера
- **readWriteAnyDatabase** - чтение/запись всех баз данных
- **userAdminAnyDatabase** - управление всеми пользователями

## Основные команды MongoDB

### Подключение к MongoDB:

```bash
# Подключение через mongosh
mongosh

# Подключение к удаленному серверу
mongosh mongodb://hostname:27017

# Подключение с аутентификацией
mongosh mongodb://username:password@hostname:27017/database
```

### Работа с базами данных:

```javascript
// Показать все базы данных
show dbs

// Использовать базу данных
use myDatabase

// Создать/использовать базу данных
use newDatabase

// Удалить текущую базу данных
db.dropDatabase()

// Получить информацию о текущей базе данных
db.stats()
```

### Работа с коллекциями:

```javascript
// Показать все коллекции
show collections

// Создать коллекцию
db.createCollection("users")

// Создать коллекцию с опциями
db.createCollection("logs", {
  capped: true,
  size: 100000,
  max: 1000
})

// Удалить коллекцию
db.collectionName.drop()

// Получить информацию о коллекции
db.collectionName.stats()
```

### Основные операции CRUD:

```javascript
// Вставка документа
db.users.insertOne({
  name: "John Doe",
  email: "john@example.com",
  age: 30
})

// Вставка нескольких документов
db.users.insertMany([
  { name: "Jane", email: "jane@example.com" },
  { name: "Bob", email: "bob@example.com" }
])

// Поиск документов
db.users.find()
db.users.findOne({ name: "John" })
db.users.find({ age: { $gte: 18 } })

// Обновление документа
db.users.updateOne(
  { name: "John" },
  { $set: { age: 31, updated: new Date() } }
)

// Обновление нескольких документов
db.users.updateMany(
  { status: "inactive" },
  { $set: { status: "active" } }
)

// Удаление документа
db.users.deleteOne({ name: "John" })

// Удаление нескольких документов
db.users.deleteMany({ age: { $lt: 18 } })
```

## Работа с документами

### Типы данных MongoDB:

```javascript
{
  // Строки
  name: "John Doe",
  
  // Числа
  age: 30,
  score: 95.5,
  
  // Булевый тип
  isActive: true,
  
  // Массивы
  tags: ["developer", "mongodb", "nosql"],
  
  // Объекты
  address: {
    street: "123 Main St",
    city: "New York",
    country: "USA"
  },
  
  // Дата
  createdAt: new Date(),
  
  // ObjectId
  _id: ObjectId("507f1f77bcf86cd799439011"),
  
  // Null
  optionalField: null
}
```

### Операторы запросов:

```javascript
// Сравнение
db.users.find({ age: { $eq: 30 } })    // равно
db.users.find({ age: { $ne: 30 } })    // не равно
db.users.find({ age: { $gt: 25 } })    // больше
db.users.find({ age: { $gte: 30 } })   // больше или равно
db.users.find({ age: { $lt: 40 } })    // меньше
db.users.find({ age: { $lte: 35 } })   // меньше или равно

// Логические
db.users.find({ $and: [{ age: { $gte: 18 } }, { age: { $lte: 65 } }] })
db.users.find({ $or: [{ age: { $lt: 18 } }, { age: { $gt: 65 } }] })
db.users.find({ $not: { age: { $lt: 18 } } })

// Массивы
db.users.find({ tags: { $in: ["developer", "admin"] } })
db.users.find({ tags: { $all: ["developer", "mongodb"] } })
db.users.find({ "address.country": "USA" })

// Регулярные выражения
db.users.find({ name: { $regex: /^J/ } })
```

### Операторы обновления:

```javascript
// Установка значений
{ $set: { field: value } }

// Увеличение числового значения
{ $inc: { counter: 1 } }

// Добавление элемента в массив
{ $push: { tags: "newTag" } }

// Добавление нескольких элементов в массив
{ $push: { tags: { $each: ["tag1", "tag2"] } } }

// Удаление элемента из массива
{ $pull: { tags: "oldTag" } }

// Удаление первого/последнего элемента массива
{ $pop: { tags: 1 } }  // 1 для последнего, -1 для первого

// Добавление уникального элемента в массив
{ $addToSet: { tags: "uniqueTag" } }
```

## Индексы и оптимизация

### Создание индексов:

```javascript
// Создание простого индекса
db.users.createIndex({ email: 1 })

// Создание составного индекса
db.users.createIndex({ firstName: 1, lastName: 1 })

// Создание уникального индекса
db.users.createIndex({ email: 1 }, { unique: true })

// Создание индекса с TTL (время жизни)
db.sessions.createIndex({ expireAt: 1 }, { expireAfterSeconds: 0 })

// Создание текстового индекса
db.articles.createIndex({ title: "text", content: "text" })

// Создание геопространственного индекса
db.places.createIndex({ location: "2dsphere" })
```

### Управление индексами:

```javascript
// Просмотр всех индексов коллекции
db.users.getIndexes()

// Удаление индекса
db.users.dropIndex("email_1")

// Удаление всех индексов (кроме _id)
db.users.dropIndexes()

// Оценка производительности запроса
db.users.find({ email: "john@example.com" }).explain("executionStats")
```

### Оптимизация запросов:

```javascript
// Использование проекции для ограничения возвращаемых полей
db.users.find({}, { name: 1, email: 1, _id: 0 })

// Ограничение количества возвращаемых документов
db.users.find().limit(10)

// Пропуск документов (для пагинации)
db.users.find().skip(10).limit(10)

// Сортировка
db.users.find().sort({ createdAt: -1 })

// Комбинация операций
db.users.find({ status: "active" })
  .sort({ createdAt: -1 })
  .skip(20)
  .limit(10)
  .explain("executionStats")
```

## Практические примеры

### Пример №1: Создание системы управления пользователями

```javascript
// Создание базы данных
use userManagement

// Создание коллекции пользователей
db.createCollection("users")

// Вставка пользователей
db.users.insertMany([
  {
    _id: ObjectId(),
    username: "john_doe",
    email: "john@example.com",
    profile: {
      firstName: "John",
      lastName: "Doe",
      age: 30,
      location: {
        city: "New York",
        country: "USA"
      }
    },
    roles: ["user"],
    preferences: {
      theme: "dark",
      notifications: true
    },
    createdAt: new Date(),
    lastLogin: new Date()
  },
  {
    _id: ObjectId(),
    username: "jane_smith",
    email: "jane@example.com",
    profile: {
      firstName: "Jane",
      lastName: "Smith",
      age: 25,
      location: {
        city: "Los Angeles",
        country: "USA"
      }
    },
    roles: ["user", "moderator"],
    preferences: {
      theme: "light",
      notifications: false
    },
    createdAt: new Date(),
    lastLogin: new Date()
  }
])

// Создание индексов для оптимизации
db.users.createIndex({ email: 1 }, { unique: true })
db.users.createIndex({ username: 1 }, { unique: true })
db.users.createIndex({ "profile.location.country": 1 })
```

### Пример №2: Запросы для управления пользователями

```javascript
// Найти пользователя по email
db.users.findOne({ email: "john@example.com" })

// Найти пользователей по стране
db.users.find({ "profile.location.country": "USA" })

// Обновить настройки пользователя
db.users.updateOne(
  { email: "john@example.com" },
  { $set: { "preferences.theme": "light", lastLogin: new Date() } }
)

// Найти пользователей с определенной ролью
db.users.find({ roles: { $in: ["moderator"] } })

// Агрегация: получить статистику по возрасту
db.users.aggregate([
  {
    $group: {
      _id: "$profile.location.country",
      averageAge: { $avg: "$profile.age" },
      userCount: { $sum: 1 }
    }
  },
  { $sort: { userCount: -1 } }
])
```

### Пример №3: Агрегационные операции

```javascript
// Простая агрегация
db.sales.aggregate([
  {
    $match: { date: { $gte: ISODate("2023-01-01") } }
  },
  {
    $group: {
      _id: { $dateToString: { format: "%Y-%m", date: "$date" } },
      totalSales: { $sum: "$amount" },
      averageSale: { $avg: "$amount" },
      saleCount: { $sum: 1 }
    }
  },
  { $sort: { _id: 1 } }
])

// Сложная агрегация с lookup
db.orders.aggregate([
  {
    $lookup: {
      from: "customers",
      localField: "customerId",
      foreignField: "_id",
      as: "customer"
    }
  },
  {
    $unwind: "$customer"
  },
  {
    $group: {
      _id: "$customer.country",
      totalOrders: { $sum: 1 },
      totalAmount: { $sum: "$amount" }
    }
  }
])
```

## Лучшие практики

### 1. Используйте подходящие идентификаторы:

```javascript
// Хорошо: используйте ObjectId для автоматических ID
{ _id: ObjectId(), name: "John" }

// Или используйте свои значения при необходимости
{ _id: "custom_id", name: "John" }
```

### 2. Проектируйте документы правильно:

- Храните вместе данные, которые обычно запрашиваются вместе
- Избегайте слишком глубоко вложенных структур
- Используйте массивы для хранения связанных данных

### 3. Создавайте эффективные индексы:

```javascript
// Создавайте индексы на полях, используемых в запросах
db.collection.createIndex({ field1: 1, field2: 1 })

// Используйте композитные индексы правильно (порядок важен)
// Поле с наивысшей селективностью должно быть первым
```

### 4. Используйте агрегацию эффективно:

```javascript
// Помещайте $match как можно раньше в pipeline
db.collection.aggregate([
  { $match: { status: "active" } },  // Сначала фильтруем
  { $lookup: { /* ... */ } },        // Потом соединяем
  { $project: { /* ... */ } }        // Потом проецируем
])
```

### 5. Обработка ошибок и безопасность:

```javascript
// Используйте валидацию схемы
db.createCollection("users", {
  validator: {
    $jsonSchema: {
      bsonType: "object",
      required: ["email", "username"],
      properties: {
        email: { bsonType: "string", pattern: "@.*\\." },
        username: { bsonType: "string", minLength: 3, maxLength: 30 }
      }
    }
  }
})
```

### 6. Мониторинг и обслуживание:

```bash
# Проверка состояния MongoDB
mongosh --eval "db.runCommand({serverStatus: 1})"

# Проверка медленных запросов
mongosh --eval "db.setProfilingLevel(2); db.system.profile.find().sort({ts: -1}).limit(5)"

# Резервное копирование
mongodump --db myDatabase --out /backup/path

# Восстановление
mongorestore /backup/path
```

---

#### 💼 Автор: Дуплей Максим Игоревич

### 📲 Контакты:

- **Telegram №1:** [@quadd4rv1n7](https://t.me/quadd4rv1n7)
- **Telegram №2:** [@dupley_maxim_1999](https://t.me/dupley_maxim_1999)

📅 **Дата:** 26.01.2026

▶️ Версия 1.0

---
> 📧 **Предложения по сотрудничеству:** maksimqwe42@mail.ru

---

### 💼 Профиль на Profi.ru
[![Profi.ru Profile](https://img.shields.io/badge/Profi.ru-Дуплей%20М.И.-FF6B35?style=for-the-badge)](https://profi.ru/profile/DupleyMI)

> Консультации и услуги программирования на платформе Profi.ru

---

### 📚 Услуги обучения
[![Обучение технологиям и языкам программирования на Kwork](https://img.shields.io/badge/Kwork-Обучение%20Программированию-blue?style=for-the-badge&logo=kwork)](https://kwork.ru/usability-testing/42465951/obuchenie-tekhnologiyam-i-yazykam-programmirovaniya)

> Профессиональное обучение технологиям и языкам программирования. Персональные консультации и курсы от опытного преподавателя.

---

### 🏫 О школе
[![Website](https://img.shields.io/badge/Maestro7IT-school--maestro7it.ru-darkgreen?style=for-the-badge)](https://school-maestro7it.ru/)

> Инновационная школа программирования, специализирующаяся на подготовке специалистов в области современных технологий и языков программирования.