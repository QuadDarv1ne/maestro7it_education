# 📦 Полный мануал по Composer: Установка, Настройка и Использование

## 📋 Содержание

1. [Введение в Composer](#введение-в-composer)
2. [Системные требования](#системные-требования)
3. [Установка Composer](#установка-composer)
4. [Базовая настройка](#базовая-настройка)
5. [Основные команды](#основные-команды)
6. [Файл composer.json](#файл-composerjson)
7. [Управление пакетами](#управление-пакетами)
8. [Работа с репозиториями](#работа-с-репозиториями)
9. [Autoloading](#autoloading)
10. [Практические примеры](#практические-примеры)
11. [Лучшие практики](#лучшие-практики)

## Введение в Composer

**Composer** — это менеджер зависимостей для языка программирования `PHP`

Он позволяет объявлять, устанавливать и обновлять зависимости вашего проекта.

`Composer` не является менеджером пакетов в традиционном смысле, он управляет зависимостями на уровне проекта.

**Основные особенности Composer:**

- Управление зависимостями проекта
- Автоматическая генерация autoloader
- Управление глобальными пакетами
- Поддержка различных источников пакетов
- Интеграция с Packagist (официальным репозиторием)

## Системные требования

### Минимальные требования:

- **PHP:** 7.2.5 или выше
- **OpenSSL:** Расширение должно быть доступно
- **JSON:** Расширение должно быть доступно
- **MBString:** Расширение должно быть доступно
- **cURL:** Рекомендуется для работы с удалёнными репозиториями

### Рекомендуемые требования:

- **PHP:** 8.0 или выше
- **Composer:** Последняя стабильная версия
- **Git:** Для установки пакетов из репозиториев
- **SSH-ключи:** Для доступа к приватным репозиториям

## Установка Composer

### Метод 1: Установка в текущую директорию

#### Для Unix-систем (Linux/macOS):

```bash
php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
php -r "if (hash_file('sha384', 'composer-setup.php') === 'dac665fdc30fdd8ec78b38b9800061b4150413ff2e3b6f88543c636f7cd84f6db9189d43a81e5503cda447da73c7e5b6') { echo 'Installer verified'; } else { echo 'Installer corrupt'; unlink('composer-setup.php'); } echo PHP_EOL;"
php composer-setup.php
php -r "unlink('composer-setup.php');"
```

### Метод 2: Глобальная установка (рекомендуется)

#### Для Unix-систем:

```bash
# Скачайте установщик
curl -sS https://getcomposer.org/installer | php

# Переместите в системную директорию
sudo mv composer.phar /usr/local/bin/composer

# Сделайте исполняемым
chmod +x /usr/local/bin/composer
```

#### Для Windows:

1. Скачайте установщик с https://getcomposer.org/Composer-Setup.exe
2. Запустите установщик как администратор
3. Убедитесь, что PHP добавлен в PATH
4. Завершите установку

### Метод 3: Установка с помощью пакетных менеджеров

#### Homebrew (macOS):

```bash
brew install composer
```

#### Chocolatey (Windows):

```cmd
choco install composer
```

#### APT (Ubuntu/Debian):

```bash
sudo apt update
sudo apt install composer
```

## Базовая настройка

### Проверка установки:

```bash
composer --version
```

### Обновление Composer:

```bash
# Для глобально установленного Composer
composer self-update

# Или если установлен локально
php composer.phar self-update
```

### Настройка глобальных параметров:

```bash
# Установка глобального каталога для пакетов
composer config --global vendor-dir /path/to/global/vendor

# Установка режима предпочтения стабильных версий
composer config --global preferred-install dist
```

## Основные команды

### Инициализация проекта:

```bash
composer init
```

### Установка зависимостей:

```bash
# Установка всех зависимостей из composer.json
composer install

# Установка зависимостей без dev-зависимостей
composer install --no-dev

# Установка с оптимизированным автолоадером
composer install --optimize-autoloader
```

### Добавление новых пакетов:

```bash
# Добавление пакета в зависимости
composer require vendor/package

# Добавление конкретной версии
composer require vendor/package:1.0.0

# Добавление пакета в dev-зависимости
composer require --dev phpunit/phpunit
```

### Обновление пакетов:

```bash
# Обновление всех пакетов
composer update

# Обновление конкретного пакета
composer update vendor/package

# Обновление с игнорированием платформенных требований
composer update --ignore-platform-reqs
```

### Удаление пакетов:

```bash
# Удаление пакета
composer remove vendor/package
```

### Генерация автолоадера:

```bash
# Перегенерация автолоадера
composer dump-autoload

# Оптимизация автолоадера для продакшена
composer dump-autoload --optimize
```

## Файл composer.json

### Минимальный composer.json:

```json
{
    "name": "vendor/project-name",
    "description": "Project description",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Author Name",
            "email": "author@example.com"
        }
    ],
    "require": {
        "php": "^8.0",
        "monolog/monolog": "^2.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
```

### Типичные секции composer.json:

#### Зависимости (require):

```json
{
    "require": {
        "php": "^8.0",
        "laravel/framework": "^9.0",
        "guzzlehttp/guzzle": "^7.0"
    }
}
```

#### Dev-зависимости (require-dev):

```json
{
    "require-dev": {
        "phpunit/phpunit": "^9.0",
        "squizlabs/php_codesniffer": "^3.0",
        "phpstan/phpstan": "^1.0"
    }
}
```

#### Autoloading:

```json
{
    "autoload": {
        "psr-4": {
            "App\\": "src/",
            "Database\\Seeders\\": "database/seeders/",
            "Database\\Factories\\": "database/factories/"
        },
        "files": [
            "src/helpers.php"
        ],
        "classmap": [
            "src/LegacyClass.php"
        ]
    }
}
```

## Управление пакетами

### Поиск пакетов:

```bash
# Поиск пакетов на Packagist
composer search monolog
```

### Просмотр информации о пакете:

```bash
# Показать информацию о пакете
composer show monolog/monolog

# Показать зависимости проекта
composer show --tree
```

### Проверка уязвимостей:

```bash
# Проверить зависимости на уязвимости
composer audit
```

### Управление кэшем:

```bash
# Показать директорию кэша
composer config cache-dir

# Очистить кэш
composer clear-cache
```

## Работа с репозиториями

### Добавление приватного репозитория:

```json
{
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/username/private-repo"
        }
    ]
}
```

### Добавление пакета из GitHub:

```bash
composer require username/repo:dev-master
```

### Использование приватных пакетов:

```json
{
    "repositories": [
        {
            "type": "composer",
            "url": "https://packagist.your-company.com"
        }
    ],
    "config": {
        "secure-http": false
    }
}
```

## Autoloading

### PSR-4 автозагрузка:

```json
{
    "autoload": {
        "psr-4": {
            "Vendor\\Package\\": "src/"
        }
    }
}
```

### PSR-0 автозагрузка:

```json
{
    "autoload": {
        "psr-0": {
            "Vendor\\Package\\": "src/"
        }
    }
}
```

### Classmap автозагрузка:

```json
{
    "autoload": {
        "classmap": ["legacy/", "lib/", "src/"]
    }
}
```

### File автозагрузка:

```json
{
    "autoload": {
        "files": ["src/functions.php"]
    }
}
```

### Пример использования автозагрузки:

```php
<?php
// composer.json
{
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}

// src/Database/Connection.php
<?php

namespace App\Database;

class Connection
{
    public function connect()
    {
        // Connection logic
    }
}

// anywhere in your code
use App\Database\Connection;

$conn = new Connection();
```

## Практические примеры

### Пример №1: Создание нового проекта с Laravel

```bash
composer create-project laravel/laravel my-project
cd my-project
composer install
```

### Пример №2: Установка конкретной версии пакета

```bash
composer require symfony/console:^5.0
```

### Пример №3: Создание библиотеки с автозагрузкой

```json
{
    "name": "vendor/my-library",
    "description": "My awesome library",
    "type": "library",
    "require": {
        "php": "^8.0"
    },
    "autoload": {
        "psr-4": {
            "MyLibrary\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Your Name",
            "email": "your.email@example.com"
        }
    ]
}
```

### Пример №4: Использование скриптов в composer.json

```json
{
    "scripts": {
        "post-install-cmd": [
            "@php artisan clear-compiled",
            "@php artisan optimize"
        ],
        "pre-update-cmd": [
            "@php artisan clear-compiled"
        ],
        "post-update-cmd": [
            "@php artisan optimize"
        ]
    }
}
```

### Пример №5: Управление версиями

```bash
# Установить точную версию
composer require vendor/package:1.2.3

# Установить любую версию 1.x
composer require vendor/package:^1.0

# Установить любую версию 1.2.x
composer require vendor/package:~1.2.0

# Установить стабильную версию
composer require vendor/package:@stable

# Установить dev-версию
composer require vendor/package:dev-master
```

## Лучшие практики

### 1. Используйте lock-файл

- Всегда коммитьте `composer.lock` в репозиторий
- Это гарантирует воспроизводимость сборки

### 2. Указывайте минимальные требования

```json
{
    "require": {
        "php": "^8.0",
        "ext-json": "*"
    }
}
```

### 3. Разделяйте зависимости

- Используйте `require` для продакшен зависимостей
- Используйте `require-dev` для dev-зависимостей

### 4. Используйте оптимизированный автолоадер в продакшене

```bash
composer install --no-dev --optimize-autoloader
```

### 5. Регулярно обновляйте зависимости

```bash
composer outdated
composer update
```

### 6. Проверяйте уязвимости

```bash
composer audit
```

### 7. Используйте семантическое версионирование

- `^1.0` - позволяет обновления до 2.0
- `~1.0` - позволяет обновления только до 1.1

---

#### 💼 Автор: Дуплей Максим Игоревич

### 📲 Контакты:

- **Telegram №1:** [@quadd4rv1n7](https://t.me/quadd4rv1n7)
- **Telegram №2:** [@dupley_maxim_1999](https://t.me/dupley_maxim_1999)

📅 **Дата:** 26.01.2026

▶️ Версия 1.0

---
> 📧 **Предложения по сотрудничеству:** maksimqwe42@mail.ru

---

### 💼 Профиль на Profi.ru
[![Profi.ru Profile](https://img.shields.io/badge/Profi.ru-Дуплей%20М.И.-FF6B35?style=for-the-badge)](https://profi.ru/profile/DupleyMI)

> Консультации и услуги программирования на платформе Profi.ru

---

### 📚 Услуги обучения
[![Обучение технологиям и языкам программирования на Kwork](https://img.shields.io/badge/Kwork-Обучение%20Программированию-blue?style=for-the-badge&logo=kwork)](https://kwork.ru/usability-testing/42465951/obuchenie-tekhnologiyam-i-yazykam-programmirovaniya)

> Профессиональное обучение технологиям и языкам программирования. Персональные консультации и курсы от опытного преподавателя.

---

### 🏫 О школе
[![Website](https://img.shields.io/badge/Maestro7IT-school--maestro7it.ru-darkgreen?style=for-the-badge)](https://school-maestro7it.ru/)

> Инновационная школа программирования, специализирующаяся на подготовке специалистов в области современных технологий и языков программирования.