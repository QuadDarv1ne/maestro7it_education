# 📘 Полный мануал по PHP: Установка, Настройка и Конфигурация

## 📋 Содержание

1. [Введение в PHP](#введение-в-php)
2. [Системные требования](#системные-требования)
3. [Методы установки PHP](#методы-установки-php)
4. [Установка на Windows](#установка-на-windows)
5. [Установка на Linux](#установка-на-linux)
6. [Установка на macOS](#установка-на-macos)
7. [Конфигурация PHP](#конфигурация-php)
8. [Управление расширениями](#управление-расширениями)
9. [Интеграция с веб-серверами](#интеграция-с-веб-серверами)
10. [Проверка установки](#проверка-установки)
11. [Практические примеры](#практические-примеры)

## Введение в PHP

PHP (рекурсивный акроним: PHP: Hypertext Preprocessor) — это популярный язык программирования общего назначения с открытым исходным кодом, особенно подходящий для веб-разработки. PHP может быть встроен в HTML и позволяет создавать динамические веб-страницы.

**Основные особенности PHP:**

- Интерпретируемый язык
- Поддерживает большинство веб-серверов
- Совместим с большинством баз данных
- Легко изучается и используется
- Множество фреймворков и библиотек

## Системные требования

### Минимальные требования:

- **Операционная система:** Windows 7+, Linux, macOS 10.12+
- **Память:** 128 МБ свободной оперативной памяти (рекомендуется 256 МБ+)
- **Место на диске:** 50-100 МБ для основной установки
- **Процессор:** Современный процессор (любой за последние 5 года)

### Рекомендуемые требования:

- **Операционная система:** Windows 10+, Ubuntu 18.04+, macOS 11+
- **Память:** 512 МБ+ свободной оперативной памяти
- **Место на диске:** 200 МБ+ свободного места
- **Процессор:** Multi-core processor

### Поддерживаемые версии PHP:

- **PHP 8.3** (актуальная стабильная)
- **PHP 8.2** (рекомендуемая LTS)
- **PHP 8.1** (поддерживается)
- **PHP 8.0** (EOL: 26 Nov 2023)

## Методы установки PHP

### 1. Установка через пакетный менеджер

- **Windows:** Chocolatey, Scoop
- **Linux:** apt, yum, dnf, pacman
- **macOS:** Homebrew, MacPorts

### 2. Установка через инсталлятор

- **Windows:** Официальный установщик с php.net
- **macOS:** Установщик с php.net или Homebrew

### 3. Установка через среды разработки

- **XAMPP** (Windows, macOS, Linux)
- **WAMP** (Windows)
- **MAMP** (Windows, macOS)
- **Laragon** (Windows)
- **LAMP** (Linux)

### 4. Компиляция из исходников

- Для продвинутых пользователей
- Полный контроль над конфигурацией

## Установка на Windows

### Метод 1: Установка с помощью Laragon (рекомендуется)

Laragon — это быстрая, легкая и мощная среда разработки для Windows.

#### Шаги установки:

1. **Скачайте Laragon:**
   - Перейдите на сайт: https://laragon.org/
   - Скачайте последнюю версию

2. **Установите Laragon:**
   - Запустите установочный файл как администратор
   - Следуйте инструкциям мастера установки
   - Установите в `C:\\laragon`

3. **Настройте PHP:**
   - После запуска Laragon автоматически установит PHP
   - Выберите нужную версию PHP в меню `PHP → Version`

4. **Проверьте установку:**
   - Откройте командную строку
   - Выполните: `php -v`

### Метод 2: Установка с помощью XAMPP

#### Шаги установки:

1. **Скачайте XAMPP:**
   - Перейдите на сайт: https://www.apachefriends.org/
   - Скачайте версию для Windows

2. **Установите XAMPP:**
   - Запустите установочный файл как администратор
   - Выберите компоненты: Apache, MySQL, PHP, phpMyAdmin
   - Завершите установку

3. **Добавьте PHP в PATH:**
   - Откройте "Системные переменные среды"
   - Найдите переменную PATH
   - Добавьте путь: `C:\\xampp\\php`

### Метод 3: Прямая установка PHP

#### Шаги установки:

1. **Скачайте PHP:**
   - Перейдите на https://windows.php.net/download/
   - Выберите Thread Safe версию для Apache или Non-Thread Safe для других серверов
   - Скачайте ZIP-архив

2. **Распакуйте PHP:**
   - Распакуйте архив в `C:\\php`
   - Убедитесь, что пути не содержат пробелов

3. **Настройте переменные среды:**
   - Добавьте `C:\\php` в системную переменную PATH
   - Перезапустите командную строку

4. **Настройте php.ini:**
   - Переименуйте `php.ini-development` в `php.ini`
   - Отредактируйте конфигурацию по необходимости

## Установка на Linux

### Ubuntu/Debian:

```bash
# Обновите список пакетов
sudo apt update

# Установите PHP и основные расширения
sudo apt install php php-cli php-fpm php-json php-common php-mysql php-zip php-gd php-mbstring php-curl php-xml php-pear php-bcmath

# Проверьте установку
php -v
```

### CentOS/RHEL/Fedora:

```bash
# Для CentOS/RHEL (с EPEL репозиторием)
sudo yum install epel-release
sudo yum install php php-cli php-fpm php-mysql php-zip php-devel php-gd php-mcrypt php-mbstring php-curl php-xml php-pear php-bcmath

# Для Fedora
sudo dnf install php php-cli php-fpm php-mysql php-zip php-devel php-gd php-mbstring php-curl php-xml php-pear php-bcmath

# Проверьте установку
php -v
```

### Arch Linux:

```bash
# Установите PHP и основные расширения
sudo pacman -S php php-apache php-fpm

# Проверьте установку
php -v
```

## Установка на macOS

### Метод 1: Установка с помощью Homebrew (рекомендуется)

```bash
# Установите Homebrew (если еще не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите PHP
brew install php

# Проверьте установку
php -v
```

### Метод 2: Прямая установка

```bash
# Скачайте и установите PHP напрямую
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
```

## Конфигурация PHP

### Файл php.ini

Файл `php.ini` — это основной файл конфигурации PHP. Он содержит настройки, влияющие на поведение PHP.

#### Расположение php.ini:

- **Windows:** `C:\\php\\php.ini` или `C:\\xampp\\php\\php.ini`
- **Linux:** `/etc/php/8.x/cli/php.ini` или `/etc/php/8.x/apache2/php.ini`
- **macOS:** `/usr/local/etc/php/8.x/php.ini`

#### Основные параметры:

```ini
; Уровень ошибок
error_reporting = E_ALL

; Отображение ошибок (только для разработки!)
display_errors = Off
display_startup_errors = Off

; Логирование ошибок
log_errors = On
error_log = /path/to/error.log

; Память
memory_limit = 256M

; Время выполнения
max_execution_time = 30
max_input_time = 60

; Загрузка файлов
upload_max_filesize = 64M
post_max_size = 64M
max_file_uploads = 20

; Безопасность
expose_php = Off
cgi.fix_pathinfo = 0

; Сессии
session.cookie_httponly = 1
session.use_strict_mode = 1

; Расширения
extension=mysqli
extension=pdo_mysql
extension=curl
extension=gd
extension=mbstring
extension=zip
```

### Типы файлов php.ini:

- **Development version:** Больше информации об ошибках, удобен для разработки
- **Production version:** Меньше информации об ошибках, безопаснее для продакшена

## Управление расширениями

### Популярные расширения PHP:

| Расширение | Назначение | Установка (Ubuntu) |
|------------|------------|--------------------|
| mysqli | Работа с MySQL | `sudo apt install php-mysqli` |
| pdo_mysql | PDO драйвер для MySQL | `sudo apt install php-pdo` |
| curl | HTTP запросы | `sudo apt install php-curl` |
| gd | Графика | `sudo apt install php-gd` |
| mbstring | Работа с многобайтовыми строками | `sudo apt install php-mbstring` |
| zip | Работа с ZIP архивами | `sudo apt install php-zip` |
| xml | Работа с XML | `sudo apt install php-xml` |
| intl | Интернационализация | `sudo apt install php-intl` |
| soap | SOAP клиент/сервер | `sudo apt install php-soap` |
| xdebug | Отладчик | `sudo apt install php-xdebug` |

### Включение/выключение расширений:

1. Откройте файл `php.ini`
2. Найдите строку с расширением (например, `;extension=mysqli`)
3. Удалите точку с запятой в начале строки для включения
4. Добавьте точку с запятой для выключения
5. Перезапустите веб-сервер

## Интеграция с веб-серверами

### Apache

#### Установка модуля PHP для Apache:

```bash
# Ubuntu/Debian
sudo apt install libapache2-mod-php

# CentOS/RHEL
sudo yum install php

# Включите модуль
sudo a2enmod php7.4  # или актуальной версии
sudo systemctl restart apache2
```

#### Пример конфигурации Apache (.htaccess):

```apache
# Указать версию PHP
AddHandler application/x-httpd-php74 .php

# Установить лимит памяти
php_value memory_limit 256M

# Включить display_errors (только для разработки!)
php_flag display_errors on
```

### Nginx

#### Установка PHP-FPM:

```bash
# Ubuntu/Debian
sudo apt install php-fpm

# Запустите сервис
sudo systemctl start php7.4-fpm  # или актуальной версии
sudo systemctl enable php7.4-fpm
```

#### Пример конфигурации Nginx:

```nginx
server {
    listen 80;
    server_name example.com;
    root /var/www/html;
    index index.php index.html;

    location ~ \\.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
    }

    location ~ /\\. {
        deny all;
    }
}
```

## Проверка установки

### Проверка версии PHP:

```bash
php -v
```

### Проверка списка расширений:

```bash
php -m
```

### Проверка местоположения php.ini:

```bash
php --ini
```

### Создание тестового файла:

Создайте файл `test.php`:
```php
<?php
phpinfo();
?>
```

Затем откройте его в браузере, чтобы увидеть информацию о конфигурации PHP.

### Тестирование командной строки:

```bash
php -r "echo 'Hello, World!';"
```

## Практические примеры

### Пример 1: Простой веб-сервер на PHP

```bash
# Запустить встроенный веб-сервер PHP
php -S localhost:8000

# С указанием маршрутизатора
php -S localhost:8000 router.php
```

### Пример 2: Проверка требований для фреймворка

Создайте файл `requirements.php`:
```php
<?php
// Проверка минимальной версии PHP
if (version_compare(PHP_VERSION, '7.4.0', '<')) {
    die('PHP 7.4.0 или выше требуется');
}

// Проверка расширений
$required_extensions = ['mysqli', 'pdo', 'curl', 'gd', 'mbstring', 'zip'];
$missing_extensions = [];

foreach ($required_extensions as $ext) {
    if (!extension_loaded($ext)) {
        $missing_extensions[] = $ext;
    }
}

if (!empty($missing_extensions)) {
    echo "Отсутствующие расширения: " . implode(', ', $missing_extensions);
} else {
    echo "Все расширения установлены!";
}
?>
```

### Пример 3: Установка Composer (менеджер пакетов PHP)

```bash
# Скачивание и установка Composer
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer

# Проверка установки
composer --version
```

---

#### 💼 Автор: Дуплей Максим Игоревич

### 📲 Контакты:

- **Telegram №1:** [@quadd4rv1n7](https://t.me/quadd4rv1n7)
- **Telegram №2:** [@dupley_maxim_1999](https://t.me/dupley_maxim_1999)

📅 Дата: 26.01.2026

▶️ Версия 1.0

---
> 📧 **Предложения по сотрудничеству:** maksimqwe42@mail.ru

---

### 💼 Профиль на Profi.ru
[![Profi.ru Profile](https://img.shields.io/badge/Profi.ru-Дуплей%20М.И.-FF6B35?style=for-the-badge)](https://profi.ru/profile/DupleyMI)

> Консультации и услуги программирования на платформе Profi.ru

---

### 📚 Услуги обучения
[![Обучение технологиям и языкам программирования на Kwork](https://img.shields.io/badge/Kwork-Обучение%20Программированию-blue?style=for-the-badge&logo=kwork)](https://kwork.ru/usability-testing/42465951/obuchenie-tekhnologiyam-i-yazykam-programmirovaniya)

> Профессиональное обучение технологиям и языкам программирования. Персональные консультации и курсы от опытного преподавателя.

---

### 🏫 О школе
[![Website](https://img.shields.io/badge/Maestro7IT-school--maestro7it.ru-darkgreen?style=for-the-badge)](https://school-maestro7it.ru/)

> Инновационная школа программирования, специализирующаяся на подготовке специалистов в области современных технологий и языков программирования.
