# 🐧 Полный мануал по командной строке Linux: Основы, Команды и Практическое Применение

## 📋 Содержание

1. [Введение в командную строку Linux](#введение-в-командную-строку-linux)
2. [Основы работы с терминалом](#основы-работы-с-терминалом)
3. [Навигация по файловой системе](#навигация-по-файловой-системе)
4. [Управление файлами и директориями](#управление-файлами-и-директориями)
5. [Поиск файлов и данных](#поиск-файлов-и-данных)
6. [Права доступа и владельцы](#права-доступа-и-владельцы)
7. [Процессы и управление задачами](#процессы-и-управление-задачами)
8. [Сетевые команды](#сетевые-команды)
9. [Управление пакетами](#управление-пакетами)
10. [Текстовые редакторы в командной строке](#текстовые-редакторы-в-командной-строке)
11. [Практические примеры для PHP разработки](#практические-примеры-для-php-разработки)
12. [Лучшие практики и советы](#лучшие-практики-и-советы)

## Введение в командную строку Linux

**Командная строка Linux (или терминал)** — это мощный интерфейс для взаимодействия с операционной системой. Она предоставляет доступ ко всем функциям системы и позволяет выполнять задачи намного быстрее и эффективнее, чем через графический интерфейс.

**Преимущества командной строки:**

- Быстрое выполнение задач
- Возможность автоматизации
- Доступ к расширенным функциям системы
- Эффективная работа с удаленными серверами
- Мощные инструменты для разработки

## Основы работы с терминалом

### Запуск терминала:

- **Ubuntu/Debian:** Ctrl+Alt+T или найдите "Terminal" в меню
- **CentOS/RHEL/Fedora:** Ctrl+Alt+T или найдите "Terminal"
- **macOS:** Откройте "Terminal" из приложений

### Основные горячие клавиши:

- `Ctrl+C` - Прервать выполнение команды
- `Ctrl+Z` - Приостановить выполнение команды
- `Ctrl+L` - Очистить экран (как команда `clear`)
- `Tab` - Автодополнение команд и путей
- `↑/↓` - Навигация по истории команд
- `Ctrl+A` - Перейти к началу строки
- `Ctrl+E` - Перейти к концу строки

### История команд:

```bash
# Просмотр истории команд
history

# Выполнение последней команды снова
!!

# Выполнение предыдущей команды с sudo
sudo !!

# Выполнение команды по номеру из истории
!123
```

## Навигация по файловой системе

### Основные команды навигации:

```bash
# Показать текущую директорию
pwd

# Показать содержимое текущей директории
ls

# Показать скрытые файлы
ls -a

# Подробный список файлов
ls -l

# Комбинированные опции
ls -la

# Перейти в домашнюю директорию
cd ~
# или просто
cd

# Перейти в родительскую директорию
cd ..

# Перейти в корневую директорию
cd /

# Перейти в конкретную директорию
cd /path/to/directory

# Перейти в предыдущую директорию
cd -
```

### Структура файловой системы Linux:

```
/
├── bin/          # Основные исполняемые файлы
├── boot/         # Файлы загрузки
├── dev/          # Файлы устройств
├── etc/          # Конфигурационные файлы
├── home/         # Домашние директории пользователей
├── lib/          # Системные библиотеки
├── media/        # Точки монтирования съемных устройств
├── mnt/          # Временные точки монтирования
├── opt/          # Дополнительное программное обеспечение
├── proc/         # Виртуальная файловая система процессов
├── root/         # Домашняя директория root пользователя
├── run/          # Временные файлы выполнения
├── sbin/         # Системные исполняемые файлы
├── srv/          # Данные для служб
├── sys/          # Виртуальная файловая система ядра
├── tmp/          # Временные файлы
├── usr/          # Пользовательские программы и данные
└── var/          # Переменные данные (логи, кэш и т.д.)
```

## Управление файлами и директориями

### Работа с файлами:

```bash
# Создать файл
touch filename.txt

# Создать директорию
mkdir directory_name

# Создать вложенную структуру директорий
mkdir -p path/to/new/directory

# Копировать файл
cp source.txt destination.txt

# Копировать директорию рекурсивно
cp -r source_directory destination_directory

# Переместить или переименовать файл
mv old_name.txt new_name.txt

# Переместить файл в другую директорию
mv file.txt /path/to/destination/

# Удалить файл
rm filename.txt

# Удалить директорию рекурсивно
rm -r directory_name

# Безопасное удаление (с подтверждением)
rm -i filename.txt

# Показать содержимое файла
cat filename.txt

# Показать содержимое файла с нумерацией строк
cat -n filename.txt

# Постраничный просмотр файла
less filename.txt

# Первые 10 строк файла
head filename.txt

# Последние 10 строк файла
tail filename.txt

# Постоянное отслеживание конца файла (полезно для логов)
tail -f /var/log/php_errors.log
```

### Работа с несколькими файлами:

```bash
# Копировать все файлы с определенным расширением
cp *.txt /backup/

# Найти и удалить все файлы с определенным расширением
find . -name "*.tmp" -delete

# Изменить права доступа для всех файлов в директории
chmod 644 *.php
```

## Поиск файлов и данных

### Поиск файлов:

```bash
# Найти файл по имени
find /path/to/search -name "filename.txt"

# Найти файлы по части имени
find /path/to/search -name "*pattern*"

# Найти файлы с определенным расширением
find /path/to/search -name "*.php"

# Найти файлы в текущей директории и поддиректориях
find . -name "config.php"

# Найти файлы по размеру
find /path/to/search -size +100M

# Найти файлы, измененные за последние 7 дней
find /path/to/search -mtime -7
```

### Поиск текста в файлах:

```bash
# Найти текст в файлах
grep "search_term" filename.txt

# Найти текст рекурсивно во всех файлах директории
grep -r "search_term" /path/to/directory/

# Найти текст без учета регистра
grep -i "search_term" filename.txt

# Найти текст и показать номера строк
grep -n "search_term" filename.txt

# Найти текст и показать окружающий контекст
grep -C 3 "search_term" filename.txt

# Использование регулярных выражений
grep -E "pattern1|pattern2" filename.txt
```

### Поиск с использованием locate:

```bash
# Обновить базу данных файлов (требуется root)
sudo updatedb

# Найти файл с помощью locate
locate filename.txt
```

## Права доступа и владельцы

### Понимание прав доступа:

```bash
# Показать права доступа
ls -l

# Пример вывода:
# -rw-r--r-- 1 user group 1234 Jan 1 12:00 file.txt
# |  |  |  |
# |  |  |  └─ Права для других пользователей
# |  |  └──── Права для группы
# |  └─────── Права для владельца
# └────────── Тип файла (- файл, d директория)
```

### Типы прав доступа:

- `r` (read) - чтение (4)
- `w` (write) - запись (2)
- `x` (execute) - выполнение (1)

### Изменение прав доступа:

```bash
# Изменить права доступа с помощью числового метода
chmod 755 filename.txt

# Изменить права доступа с помощью букв
chmod u+rwx,g+rx,o+rx filename.txt

# Наиболее распространенные права:
# 755 - владелец: rwx, группа: rx, другие: rx (для исполняемых файлов)
# 644 - владелец: rw, группа: r, другие: r (для обычных файлов)
# 600 - владелец: rw, группа: -, другие: - (для приватных файлов)
# 777 - все права для всех (НЕ РЕКОМЕНДУЕТСЯ)
```

### Изменение владельца и группы:

```bash
# Изменить владельца файла
chown username filename.txt

# Изменить группу файла
chgrp groupname filename.txt

# Изменить владельца и группу рекурсивно
chown -R username:groupname directory/
```

## Процессы и управление задачами

### Просмотр процессов:

```bash
# Показать все запущенные процессы
ps aux

# Показать дерево процессов
pstree

# Постоянный мониторинг процессов
top
# или более современный вариант
htop

# Показать процессы, связанные с определенным словом
ps aux | grep php
```

### Управление процессами:

```bash
# Запустить процесс в фоне
command &

# Показать фоновые задачи
jobs

# Приостановить выполнение процесса (Ctrl+Z)
# Затем можно:
bg    # продолжить в фоне
fg    # вернуть в передний план

# Убить процесс по PID
kill PID

# Убить процесс по имени
pkill -f process_name

# Убить процесс жестко
kill -9 PID

# Найти PID процесса
pidof process_name
```

### Мониторинг ресурсов:

```bash
# Мониторинг использования CPU и памяти
top
htop

# Показать использование диска
df -h

# Показать использование памяти
free -h

# Показать использование диска в текущей директории
du -sh .

# Показать использование диска для поддиректорий
du -sh */  # только в текущей директории
```

## Сетевые команды

### Проверка сетевых подключений:

```bash
# Проверить IP адрес
ip addr show
# или старый способ
ifconfig

# Проверить маршрут по умолчанию
ip route show

# Проверить соединение с удаленным сервером
ping google.com

# Проверить маршрут к удаленному серверу
traceroute google.com

# Проверить открытые порты
netstat -tuln
ss -tuln

# Проверить соединения с определенным портом
netstat -an | grep :80
```

### Работа с сетью:

```bash
# Скачать файл с интернета
wget https://example.com/file.zip

# Скачать файл с помощью curl
curl -O https://example.com/file.zip

# Проверить доступность веб-сайта
curl -I https://example.com

# Отправить POST запрос с данными
curl -X POST -d "param=value" https://example.com/api
```

## Управление пакетами

### Ubuntu/Debian (APT):

```bash
# Обновить список пакетов
sudo apt update

# Обновить установленные пакеты
sudo apt upgrade

# Установить пакет
sudo apt install package_name

# Удалить пакет
sudo apt remove package_name

# Полностью удалить пакет с конфигурацией
sudo apt purge package_name

# Поиск пакета
apt search package_name

# Показать информацию о пакете
apt show package_name
```

### CentOS/RHEL/Fedora (YUM/DNF):

```bash
# Обновить систему (CentOS/RHEL)
sudo yum update

# Установить пакет (CentOS/RHEL)
sudo yum install package_name

# Удалить пакет (CentOS/RHEL)
sudo yum remove package_name

# Обновить систему (Fedora)
sudo dnf update

# Установить пакет (Fedora)
sudo dnf install package_name

# Поиск пакета
dnf search package_name
```

## Текстовые редакторы в командной строке

### nano (простой редактор):

```bash
# Открыть файл в nano
nano filename.txt

# Горячие клавиши в nano:
# Ctrl+O - сохранить файл
# Ctrl+X - выйти из редактора
# Ctrl+W - найти текст
# Ctrl+\ - заменить текст
```

### vim (мощный редактор):

```bash
# Открыть файл в vim
vim filename.txt

# Режимы vim:
# Command mode - навигация и команды (Esc для перехода)
# Insert mode - ввод текста (i, a, o для входа)
# Visual mode - выделение текста (v для входа)

# Основные команды vim:
# :w - сохранить
# :q - выйти
# :wq или :x - сохранить и выйти
# :q! - выйти без сохранения
# dd - удалить строку
# yy - скопировать строку
# p - вставить
# u - отменить
# Ctrl+R - повторить
# /text - поиск текста
# n - следующее вхождение
# N - предыдущее вхождение
```

## Практические примеры для PHP разработки

### Работа с PHP:

```bash
# Проверить версию PHP
php -v

# Выполнить PHP скрипт
php script.php

# Запустить встроенный веб-сервер PHP
php -S localhost:8000

# Проверить синтаксис PHP файла
php -l script.php

# Запустить интерактивный режим PHP
php -a
```

### Работа с Composer:

```bash
# Установить зависимости
composer install

# Обновить зависимости
composer update

# Добавить новый пакет
composer require vendor/package

# Удалить пакет
composer remove vendor/package

# Установить Composer глобально
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
```

### Работа с базами данных:

```bash
# Подключиться к MySQL
mysql -u username -p

# Подключиться к PostgreSQL
psql -U username -d database_name

# Импортировать SQL файл в MySQL
mysql -u username -p database_name < backup.sql

# Импортировать SQL файл в PostgreSQL
psql -U username -d database_name < backup.sql

# Создать дамп базы данных MySQL
mysqldump -u username -p database_name > backup.sql

# Создать дамп базы данных PostgreSQL
pg_dump -U username -d database_name > backup.sql
```

### Работа с Git:

```bash
# Инициализировать репозиторий
git init

# Клонировать репозиторий
git clone https://github.com/user/repo.git

# Проверить статус
git status

# Добавить файлы для коммита
git add .

# Сделать коммит
git commit -m "Commit message"

# Отправить изменения
git push origin main

# Получить изменения
git pull origin main

# Создать и переключиться на новую ветку
git checkout -b feature-branch

# Объединить ветки
git merge branch-name
```

### Управление сервисами:

```bash
# Запустить Apache
sudo systemctl start apache2
# или
sudo service apache2 start

# Остановить Apache
sudo systemctl stop apache2

# Перезапустить Apache
sudo systemctl restart apache2

# Проверить статус Apache
sudo systemctl status apache2

# Включить автозапуск Apache
sudo systemctl enable apache2

# Аналогично для других сервисов (nginx, mysql, php-fpm и т.д.)
```

### Работа с логами:

```bash
# Просмотреть последние 100 строк лога Apache
sudo tail -100 /var/log/apache2/error.log

# Отслеживать лог в реальном времени
sudo tail -f /var/log/apache2/access.log

# Найти ошибки 500 в логе
sudo grep " 500 " /var/log/apache2/access.log

# Найти ошибки PHP в логе
sudo grep "PHP" /var/log/php_errors.log
```

## Лучшие практики и советы

### 1. Используйте алиасы для часто используемых команд:

```bash
# Добавить в ~/.bashrc или ~/.zshrc
alias ll='ls -la'
alias la='ls -A'
alias ..='cd ..'
alias ...='cd ../..'

# Для работы с PHP проектами
alias art='php artisan'  # Laravel
alias composer='php composer.phar'
```

### 2. Используйте табуляцию для автодополнения:

- Начните вводить команду или путь
- Нажмите `Tab` для автодополнения
- Если несколько вариантов, нажмите `Tab` дважды

### 3. Используйте pipe (|) для объединения команд:

```bash
# Найти PHP файлы и показать их размер
find . -name "*.php" -exec du -h {} \;

# Найти процесс и убить его
ps aux | grep php | awk '{print $2}' | xargs kill

# Посчитать количество файлов
ls -la | wc -l
```

### 4. Используйте wildcards (шаблоны):

```bash
# Все файлы с определенным расширением
*.php
*.js
*.css

# Файлы с определенным шаблоном
config_*.php
*.conf
test?.php  # test1.php, test2.php, но не test10.php
```

### 5. Безопасность:

```bash
# Не выполняйте команды с sudo без понимания их действия
# Проверяйте команды перед выполнением

# Избегайте chmod 777
# Используйте минимально необходимые права

# Будьте осторожны с командами удаления:
# rm -rf /  # ЭТА КОМАНДА ОПАСНА - НЕ ВЫПОЛНЯЙТЕ ЕЁ
```

### 6. Полезные команды для диагностики:

```bash
# Проверить использование диска
df -h

# Проверить использование памяти
free -h

# Проверить использование CPU
top
iostat  # если установлен

# Проверить сетевые соединения
netstat -tuln

# Проверить температуру системы (если поддерживается)
sensors  # если установлен
```

---

#### 💼 Автор: Дуплей Максим Игоревич

### 📲 Контакты:

- **Telegram №1:** [@quadd4rv1n7](https://t.me/quadd4rv1n7)
- **Telegram №2:** [@dupley_maxim_1999](https://t.me/dupley_maxim_1999)

📅 **Дата:** 26.01.2026

▶️ Версия 1.0

---
> 📧 **Предложения по сотрудничеству:** maksimqwe42@mail.ru

---

### 💼 Профиль на Profi.ru
[![Profi.ru Profile](https://img.shields.io/badge/Profi.ru-Дуплей%20М.И.-FF6B35?style=for-the-badge)](https://profi.ru/profile/DupleyMI)

> Консультации и услуги программирования на платформе Profi.ru

---

### 📚 Услуги обучения
[![Обучение технологиям и языкам программирования на Kwork](https://img.shields.io/badge/Kwork-Обучение%20Программированию-blue?style=for-the-badge&logo=kwork)](https://kwork.ru/usability-testing/42465951/obuchenie-tekhnologiyam-i-yazykam-programmirovaniya)

> Профессиональное обучение технологиям и языкам программирования. Персональные консультации и курсы от опытного преподавателя.

---

### 🏫 О школе
[![Website](https://img.shields.io/badge/Maestro7IT-school--maestro7it.ru-darkgreen?style=for-the-badge)](https://school-maestro7it.ru/)

> Инновационная школа программирования, специализирующаяся на подготовке специалистов в области современных технологий и языков программирования.