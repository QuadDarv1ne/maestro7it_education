# 🎨 Полное руководство по установке Scoop, Windows-screenFetch и другим терминальным украшениям в Windows

> 📚 **Подробное руководство** для тех, кто хочет превратить стандартную командную строку Windows в удобную и красивую среду разработки

## 📖 Оглавление

1. [Что вы узнаете из этого руководства](#-что-вы-узнаете-из-этого-руководства)
2. [Практические сценарии использования](#-практические-сценарии-использования)
3. [Что такое Scoop и зачем он нужен](#-что-такое-scoop-и-зачем-он-нужен)
4. [Подготовка PowerShell](#-подготовка-powershell)
5. [Установка Scoop](#-установка-scoop-два-способа)
6. [Расширенная установка](#-расширенная-установка-другие-пути-прокси-тихая-установка)
7. [Базовая настройка Scoop](#-базовая-настройка-scoop-после-установки)
8. [Установка Windows-screenFetch](#-установка-windows-screenfetch)
9. [Другие аналоги screenFetch](#-другие-аналоги-screenfetchneofetch-для-windows)
10. [Сравнительная таблица](#-сравнительная-таблица)
11. [Интересные дополнения](#-интересные-дополнения-программы-которые-сделают-терминал-круче)
12. [Кастомизация оболочки](#-кастомизация-оболочки-powershell)
13. [Развлечения](#-просто-по-приколу-развлечения)
14. [Возможные проблемы](#-возможные-проблемы-и-их-решение)
15. [Выводы](#-выводы)

---

Этот материал — подробная инструкция для тех, кто хочет превратить стандартную командную строку Windows в удобную и красивую среду разработки.

Мы разберём установку менеджера пакетов Scoop, настройку аналогов screenFetch для Windows, а также десятки полезных программ, которые сделают работу в терминале быстрой и приятной.

## Что вы узнаете из этого руководства

- Как установить и настроить Scoop — современный менеджер пакетов для Windows
- Как превратить терминал в информативную панель с системной информацией
- Как заменить устаревшие команды современными альтернативами
- Как кастомизировать внешний вид PowerShell
- Как избежать распространённых ошибок при установке

## 📋 Практические сценарии использования

### Сценарий №1: Быстрая настройка нового компьютера

```powershell
# Установка Scoop
irm get.scoop.sh | iex

# Базовая настройка
scoop install git aria2
scoop bucket add extras

# Установка основных инструментов
scoop install 7zip vscode git python nodejs

# Кастомизация терминала
scoop install oh-my-posh
oh-my-posh init pwsh --config "$(scoop prefix oh-my-posh)\themes\jandedobbeleer.omp.json" | Invoke-Expression
```

### Сценарий №2: Настройка для разработчика Python

```powershell
# Установка Python и инструментов
scoop install python pipenv poetry

# Установка современных утилит
scoop install bat fd ripgrep

# Настройка автодополнения
Install-Module PSReadLine -Force
Set-PSReadLineOption -PredictionSource History
```

### Сценарий №3: Настройка для веб-разработчика

```powershell
# Установка Node.js и инструментов
scoop install nodejs npm yarn pnpm

# Установка веб-серверов
scoop install nginx apache

# Установка баз данных
scoop install mysql postgresql redis

# Современные инструменты
scoop install eza bat zoxide
```

### Сценарий №4: Максимальная информативность терминала

```powershell
# Установка всех информаторов
scoop install Scoop4kariiin/Windows-screenFetch winfetch fastfetch

# Установка системных мониторов
scoop install btop htop

# Установка утилит для информации
scoop install neofetch lsd exa

# Запуск информатора
fastfetch --config "C:\Users\$env:USERNAME\.config\fastfetch\config.jsonc"
```

---

## 📦 Что такое Scoop и зачем он нужен?

**Scoop** — это менеджер пакетов для Windows, работающий из командной строки. Он позволяет устанавливать программы одной командой без прав администратора, не захламляя реестр и системные папки. Всё ставится в ваш профиль (`%USERPROFILE%\scoop`).

> 💡 **Для кого Scoop:** разработчики, системные администраторы, power users, которые часто устанавливают и тестируют ПО

### Преимущества Scoop:

- 🔍 **Не нужно искать установщики в браузере**
- 🔄 **Простое обновление**: `scoop update *`
- 🧹 **Чистое удаление**: `scoop uninstall <имя>`
- 📋 **Программы не оставляют следов в реестре**
- 📦 **Можно установить несколько версий одного приложения**

---

## 🛠 Подготовка PowerShell

Scoop — это скрипт PowerShell, поэтому сначала убедимся, что среда готова.

> ⚡ **Совет:** Все команды в этом руководстве можно копировать и вставлять напрямую в PowerShell

### 1.1 Проверка версии PowerShell

```powershell
$PSVersionTable.PSVersion
```

> **Важно**: Scoop работает в Windows PowerShell 5.1 и PowerShell 7+. Если у вас версия ниже 5.1, обновите систему или установите PowerShell 7.

### 1.2 Разрешаем выполнение локальных скриптов

По умолчанию PowerShell запрещает запуск скриптов. Это легко исправить:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

> Команда выполняется один раз. Политика `RemoteSigned` разрешает выполнять ваши собственные скрипты и доверенные скрипты из интернета.

### 1.3 Проверка языка PowerShell

Scoop требует режим `FullLanguage`. Проверьте:

```powershell
$ExecutionContext.SessionState.LanguageMode
```

Должно быть `FullLanguage`. Если нет — вероятно, у вас ограниченная версия (например, в Windows 10/11 S Mode или из-за корпоративных политик).

---

## Установка Scoop (два способа)

> 📝 **Примечание:** Выберите один способ установки. Первый рекомендуется для большинства пользователей.

### ✅ Способ №1. Обычная установка (рекомендуется) — без прав администратора

Самый безопасный и правильный метод. **Не запускайте PowerShell от имени Администратора!**

1. Откройте обычный PowerShell (через поиск или `Win+R` → `powershell`)
2. Вставьте одну команду:

```powershell
irm get.scoop.sh | iex
```

3. Дождитесь завершения. Scoop установится в папку `C:\Users\<Ваше_Имя>\scoop`
4. Перезапустите PowerShell (или выполните `refreshenv`)
5. Проверьте: `scoop --version`

### ⚠️ Способ №2. Установка с правами администратора (когда очень надо)

Разработчики намеренно отключили возможность установки из-под Администратора обычной командой — это мера безопасности. Однако, если вы точно понимаете, что делаете (например, в изолированной среде или CI), можно принудительно запустить установку с флагом `-RunAsAdmin`.

**Односрочная команда для PowerShell (Администратор):**

```powershell
iex "& {$(irm get.scoop.sh)} -RunAsAdmin"
```

**Или по шагам (для контроля):**

```powershell
# Скачиваем скрипт установки
irm get.scoop.sh -outfile 'install.ps1'
# Запускаем с флагом администратора
.\install.ps1 -RunAsAdmin
```

> ⚠️ **Внимание**: устанавливая Scoop под администратором, вы даёте программам больше прав, чем им нужно. Старайтесь использовать обычный режим.

---

## 📂 Расширенная установка (другие пути, прокси, тихая установка)

> 🛡️ **Для корпоративных пользователей:** раздел полезен при работе в корпоративных сетях с прокси

### 1. Изменение папки установки:

```powershell
# Временно задать переменную
$env:SCOOP='D:\ScoopApps'
# Запустить установку
irm get.scoop.sh | iex
```

### 2. Для глобальных приложений (требуют админ-прав):

```powershell
$env:SCOOP_GLOBAL='E:\GlobalScoop'
[Environment]::SetEnvironmentVariable('SCOOP_GLOBAL', $env:SCOOP_GLOBAL, 'Machine')
irm get.scoop.sh | iex
```

### 3. Установка через прокси:

```powershell
irm get.scoop.sh -Proxy 'http://192.168.1.1:8080' | iex
```

### 4. Тихая установка (без вывода сообщений):

```powershell
.\install.ps1 -RunAsAdmin | Out-Null
```

> Код возврата `$LASTEXITCODE` — 0 при успехе.

---

## 🔧 Базовая настройка Scoop после установки

> **Рекомендуется выполнить:** эти шаги значительно улучшат производительность и функциональность Scoop

### 2.1 Установка Git

Git необходим для добавления пользовательских репозиториев (бакетов):

```powershell
scoop install git
```

### 2.2 Ускорение загрузки (aria2)

Scoop поддерживает многопоточную загрузку через aria2:

```powershell
scoop install aria2
scoop config aria2-enabled true
```

При желании можно настроить количество потоков: 
```powershell
scoop config aria2-max-connection-per-server 8
```

### 2.3 Добавление популярных бакетов

Бакет — это коллекция манифестов приложений. Основные:

```powershell
scoop bucket add extras      # Дополнительные приложения
scoop bucket add versions    # Старые версии (nodejs12, python27 и т.д.)
scoop bucket add nirsoft     # Утилиты NirSoft
scoop bucket add sysinternals # Инструменты Sysinternals
scoop bucket add nonportable # Программы, требующие прав администратора
```

---

## 🖥 Установка Windows-screenFetch

> **Популярная утилита:** выводит красивую системную информацию с ASCII-артом

**Windows-screenFetch** — это порт знаменитого скрипта screenFetch для PowerShell. Он выводит ASCII-логотип Windows и информацию о системе.

### ✅ Способ №1. Через Scoop (рекомендуется)

Добавьте бакет Scoop4kariiin (автор поддерживает множество полезных утилит):

```powershell
scoop bucket add Scoop4kariiin https://github.com/AkariiinMKII/Scoop4kariiin
```

Установите Windows-screenFetch:

```powershell
scoop install Scoop4kariiin/Windows-screenFetch
```

**Запускайте:**

```powershell
screenFetch -Distro win11   # Логотип Windows 11
screenFetch -Distro win10   # Логотип Windows 10
screenFetch -Distro mac     # Можно даже логотип macOS :)
```

### 📜 Способ №2. Ручная установка (без Scoop)

Если по каким-то причинам вы не используете Scoop, можно установить скрипт напрямую из GitHub:

```powershell
git clone https://github.com/AkariiinMKII/Windows-screenFetch
cd Windows-screenFetch
# Импортировать модуль в сессию
Import-Module .\screenfetch.psd1
# Или добавить в профиль:
notepad $PROFILE
# Добавить строку: Import-Module "путь\к\Windows-screenFetch\screenfetch.psd1"
```

---

## 🌟 Другие аналоги screenFetch/neofetch для Windows

> 🔍 **Сравнение:** выберите инструмент, который лучше подходит под ваши нужды

### 🥇 1. winfetch (Go)

Быстрый отдельный .exe, написан на Go. Не требует PowerShell.

**Установка:**

```powershell
scoop install winfetch
```

или скачайте вручную с GitHub Releases.

**Запуск:** `winfetch`

### 🥈 2. AeroFetch (PowerShell 7+)

Современный модуль, активно обновляется (последний релиз — июль 2025). Требует PowerShell 7.

**Установка:**

```powershell
Install-Module -Name AeroFetch -AllowPrerelease
```

**Обновление:** `Update-Module -Name AeroFetch`

**Запуск:** `AeroFetch`

### 🥉 3. fastfetch (C)

Быстрая, кроссплатформенная программа на C. Работает на Windows, Linux, macOS. Выдаёт огромное количество информации, поддерживает кастомные логотипы.

**Установка через Scoop:**

```powershell
scoop install fastfetch
```

**Запуск:** `fastfetch`

### 🔹 4. zeitfetch (Rust)

Лёгкая утилита на Rust, единая кодовая база для всех ОС.

**Установка:**

```powershell
scoop install zeitfetch
```

**Запуск:** `zeitfetch`

### 🔸 5. neofetch (через WSL)

Оригинальный neofetch для Linux можно запустить в WSL. Если у вас установлен WSL, просто выполните `wsl neofetch`.

---

## 📊 Сравнительная таблица

| Инструмент | Язык/Тип | Особенности | Установка через Scoop | Актуальность | Скорость |
|------------|----------|-------------|----------------------|--------------|----------|
| Windows-screenFetch | PowerShell | Много логотипов, легкий | ✅ | 2024 | ⭐⭐⭐ |
| winfetch | Go (EXE) | Очень быстрый, независимый | ✅ | 2021 (стабилен) | ⭐⭐⭐⭐⭐ |
| AeroFetch | PowerShell 7+ | Современный, активная разработка | ❌ (модуль) | 2025 | ⭐⭐⭐⭐ |
| fastfetch | C | Максимум информации, конфигурируемый | ✅ | 2026 | ⭐⭐⭐⭐⭐ |
| zeitfetch | Rust | Минималистичный, кроссплатформенный | ✅ | 2023 | ⭐⭐⭐⭐ |

**Рекомендация:** 
- Для красивого лого и базовых данных — **Windows-screenFetch** или **fastfetch**
- Для максимальной информативности — **fastfetch**
- Для PowerShell 7 — **AeroFetch**

---

## Интересные дополнения: программы, которые сделают терминал круче

> **Productivity boost:** эти утилиты могут сэкономить часы ежедневной работы

Scoop позволяет установить десятки утилит, превращающих скучную командную строку в современный инструмент разработки.

### 📁 Современные аналоги классических команд

| Команда | Замена | Описание | Установка | Пример использования |
|---------|--------|----------|-----------|---------------------|
| `ls` | `eza` / `lsd` | Красивый вывод с иконками, цветами | `scoop install eza` | `eza -la --icons --git` |
| `cat` | `bat` | Подсветка синтаксиса, номера строк | `scoop install bat` | `bat --style=numbers,changes README.md` |
| `cd` | `zoxide` | Умная навигация (пропускайте `cd`) | `scoop install zoxide` | `z add D:\Projects\MyProject` затем `MyProject` |
| `find` | `fd` | Быстрый поиск файлов (интуитивный) | `scoop install fd` | `fd ".py$" src --exec wc -l` |
| `grep` | `ripgrep` | Молниеносный поиск в тексте | `scoop install ripgrep` | `rg "function.*{" --type js` |
| `du` | `duf` | Графическое отображение занятого места | `scoop install duf` | `duf --only fs` |
| `top` | `btop` | Красивый системный монитор | `scoop install btop` | `btop --utf-force` |
| `man` | `tldr` | Краткая документация по командам | `scoop install tldr` | `tldr git` |

**Пример использования:**

```powershell
eza -la --icons
bat config.json
zoxide add D:\Projects  # затем просто "projects"
fd .md src
rg "function" *.js
duf
btop
tldr ffmpeg
```

---

## Кастомизация оболочки (PowerShell)

> 🎭 **Персонализация:** сделайте ваш терминал уникальным и удобным для работы

Сделайте приглашение красивым и информативным.

### 1. Oh My Posh (рекомендуется)

```powershell
scoop install oh-my-posh
# Добавить в профиль PowerShell:
notepad $PROFILE
# Строка: oh-my-posh init pwsh --config "$(scoop prefix oh-my-posh)\themes\jandedobbeleer.omp.json" | Invoke-Expression
```

### 2. Starship (минималистичный, быстрый)

```powershell
scoop install starship
# В профиль: Invoke-Expression (&starship init powershell)
```

### 3. PSReadLine — улучшение истории и автодополнения

```powershell
Install-Module PSReadLine -Force
```

**Настройки в профиле:**

```powershell
Set-PSReadLineOption -PredictionSource History
Set-PSReadLineOption -PredictionViewStyle ListView
```

### 4. Terminal-Icons — иконки для файлов в ls

```powershell
Install-Module Terminal-Icons
Import-Module Terminal-Icons  # и добавьте в профиль
```

---

## 🤖 Просто «по приколу» (развлечения)

> 🎉 **Fun section:** весёлые утилиты для развлечения и креативности

```powershell
scoop install lolcat   # Радужный текст (требует Python)
scoop install cowsay   # Коровка говорит
scoop install fortune  # Случайные цитаты
scoop install cmatrix  # Матрица в консоли
scoop install sl       # Паровозик, если ошиблись в ls
```

**Пример весёлой команды:**

```powershell
fortune | cowsay | lolcat
```

---

## ⚠️ Возможные проблемы и их решение

> 🆘 **Troubleshooting:** если что-то пошло не так, найдите решение здесь

### ❌ Ошибка «Running the installer as administrator is disabled by default»

**Причина:** запуск установки из PowerShell с правами администратора.  
**Решение:** либо используйте обычный PowerShell (без админа), либо добавьте флаг `-RunAsAdmin` (см. Способ 2).

### ❌ Ошибка «... cannot be loaded because running scripts is disabled»

**Причина:** политика выполнения скриптов запрещает их запуск.  
**Решение:** выполните `Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`.

### ❌ Scoop не видит команду после установки

**Причина:** не обновились переменные окружения.  
**Решение:** перезапустите PowerShell или выполните `refreshenv` (если установлено через Scoop).

### ❌ Не удаётся добавить бакет («git is required»)

**Причина:** не установлен Git.  
**Решение:** `scoop install git`.

### ❌ Медленная загрузка / ошибка скачивания

**Причина:** GitHub может быть заблокирован или медленный.  
**Решение:** установите `aria2` и включите его. Или используйте зеркала/прокси.

### ❌ Программа не запускается из-за отсутствия DLL

**Причина:** некоторые программы требуют Visual C++ Redistributable.  
**Решение:** `scoop install vcredist` (установит нужные рантаймы).

---

## 🏁 Выводы

Теперь у вас есть полноценная среда для работы в командной строке Windows.

**Вы умеете:**

✅ Устанавливать Scoop с правами администратора и без  
✅ Ставить Windows-screenFetch и другие красивые информаторы  
✅ Добавлять современные утилиты, которые экономят часы времени  
✅ Настраивать внешний вид PowerShell до неузнаваемости  

Экспериментируйте, добавляйте новые бакеты, следите за обновлениями сообщества Scoop. 

> **Помните:** главное преимущество такого подхода — чистота системы и скорость получения инструментов.

---

## 📞 Обратная связь

Если у вас есть вопросы или предложения по улучшению руководства, создайте issue в репозитории проекта.

---

## 🔗 Полезные ссылки

- [Официальный сайт Scoop](https://scoop.sh)
- [Поиск приложений в Scoop](https://scoop.netlify.app)
- [Репозиторий Windows-screenFetch](https://github.com/AkariiinMKII/Windows-screenFetch)
- [Fastfetch для Windows](https://github.com/fastfetch-cli/fastfetch)

## 📁 Дополнительные проекты

В эту директорию были добавлены следующие проекты:

### MTProxy

**MTProxy** — это высокопроизводительный прокси-сервер для протокола `MTProto`, используемого `Telegram` для обеспечения безопасной связи между клиентами и серверами `Telegram`. Позволяет обходить блокировки и фильтрацию, обеспечивая быстрый и надежный доступ к сервисам `Telegram`.

**Основные возможности:**
- ⚡ Высокая производительность и низкая задержка
- 🔒 Полная совместимость с протоколом `MTProto`
- 🛡️ Защита от блокировок через случайное заполнение
- 👥 Поддержка нескольких секретных ключей
- 📊 Интегрированная система статистики

**Недавние улучшения:**

В рамках оптимизации производительности были добавлены следующие компоненты:

- **memory-pool.h/memory-pool.c** — реализация пула памяти для снижения накладных расходов на выделение памяти
- **ddos-protection-enhanced.h/ddos-protection-enhanced.c** — улучшенная система защиты от DDoS-атак с ограничением скорости
- **protocol-manager.h/protocol-manager.c** — расширяемый менеджер протоколов для поддержки различных протоколов (MTProto, Shadowsocks, SOCKS5, HTTP Connect)
- **load-balancer.h/load-balancer.c** — система балансировки нагрузки для распределения запросов между несколькими серверами

### Scoop4kariiin

**Scoop4kariiin** — это бакет для [Scoop](https://github.com/ScoopInstaller/Scoop), установщика командной строки для Windows. Содержит различные манифесты приложений, которые можно легко установить с помощью Scoop.

**Для установки приложений из этого бакета:**

```PowerShell
# Добавить этот бакет к вашему Scoop
scoop bucket add Scoop4kariiin https://github.com/AkariiinMKII/Scoop4kariiin

# Установить приложения по имени манифеста
scoop install <manifest>
```

> [!IMPORTANT]
> Некоторые манифесты используют пользовательские функции, хранящиеся в модуле PowerShell под названием Scoop4kariiinUtils. Без этого модуля пользовательские скрипты могут завершаться с ошибками. Установите модуль с помощью команды: `scoop install Scoop4kariiin/Scoop4kariiinUtils`

---

### 💼 Профиль на Profi.ru

[![Profi.ru Profile](https://img.shields.io/badge/Profi.ru-Дуплей%20М.И.-FF6B35?style=for-the-badge)](https://profi.ru/profile/DupleyMI)

> Консультации и услуги программирования на платформе Profi.ru

---

### 📚 Услуги обучения

[![Обучение технологиям и языкам программирования на Kwork](https://img.shields.io/badge/Kwork-Обучение%20Программированию-blue?style=for-the-badge&logo=kwork)](https://kwork.ru/usability-testing/42465951/obuchenie-tekhnologiyam-i-yazykam-programmirovaniya)

> Профессиональное обучение технологиям и языкам программирования. Персональные консультации и курсы от опытного преподавателя.

---

### 🏫 О школе

[![Website](https://img.shields.io/badge/Maestro7IT-school--maestro7it.ru-darkgreen?style=for-the-badge)](https://school-maestro7it.ru/)

> Инновационная школа программирования, специализирующаяся на подготовке специалистов в области современных технологий и языков программирования.

---

💼 **Автор:** Дуплей Максим Игоревич

📲 **Telegram №1:** [@quadd4rv1n7](https://t.me/quadd4rv1n7)

📲 **Telegram №2:** [@dupley_maxim_1999](https://t.me/dupley_maxim_1999)

📅 **Дата последнего обновления:** 13.02.2026
