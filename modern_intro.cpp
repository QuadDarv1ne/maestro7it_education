/**
 * @file modern_cpp_basics_demo.cpp
 * @brief Демонстрация основ современного C++ для начинающих
 *
 * Этот учебный пример показывает, как писать простой, безопасный
 * и легко читаемый код на C++ с использованием трёх ключевых
 * возможностей стандарта C++11 и новее:
 *
 *   1. std::string — безопасная и гибкая работа со строками
 *      (в отличие от устаревших char[] и указателей).
 *   2. auto — автоматический вывод типа переменной,
 *      уменьшающий дублирование и повышающий читаемость.
 *   3. Range-based for — современный цикл «для каждого элемента»,
 *      исключающий ошибки с индексами и границами.
 *
 * Особенности реализации:
 *   • Программа автоматически настраивает консоль Windows
 *     на кодировку UTF-8 (через SetConsoleOutputCP).
 *   • Для анализа символов используется простая проверка
 *     диапазонов латинских букв (A–Z, a–z), чтобы избежать
 *     некорректного использования std::isalpha с UTF-8.
 *   • Код кроссплатформенный: компилируется и работает
 *     на Windows, Linux и macOS без изменений.
 *
 * Рекомендации по компиляции:
 *   g++ -std=c++17 -Wall -O2 modern_cpp_basics_demo.cpp -o demo
 *
 * Примечание для преподавателей:
 *   Тема Unicode и полноценной обработки кириллицы вынесена
 *   в отдельный модуль курса («Работа с кодировками»).
 *   Здесь фокус — на синтаксисе и стиле современного C++.
 *
 * @author Дуплей М.И., основатель школы Maestro7IT
 * @date Октябрь 2025
 */

#include <iostream>
#include <string>

// Автоматическая настройка UTF-8 для консоли Windows
#ifdef _WIN32
    #include <windows.h>
    namespace {
        // Анонимный namespace гарантирует, что инициализация произойдёт один раз
        const bool utf8_setup = []() {
            SetConsoleOutputCP(CP_UTF8);
            SetConsoleCP(CP_UTF8);
            return true;
        }();
    }
#endif

int main() {
    // Пример строки: используем латиницу для корректной и простой обработки
    std::string текст = "Modern C++ is clear, safe, and powerful.\n Современный C++ понятен, безопасен и мощен.";

    // 1. Вывод строки целиком
    std::cout << "🔤 Исходная строка:\n   " << текст << "\n\n";

    // 2. Перебор всех символов с помощью range-based for и auto
    std::cout << "🔍 Каждый символ по отдельности:\n";
    for (auto символ : текст) {
        std::cout << "   [" << символ << "]\n";
    }
    std::cout << "\n";

    // 3. Фильтрация: собираем только латинские буквы
    std::string только_буквы;
    int количество_букв = 0;

    for (auto c : текст) {
        if ((c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z')) {
            только_буквы += c;
            ++количество_букв;
        }
    }

    // 4. Вывод результата
    std::cout << "🔤 Только буквы: " << только_буквы << "\n";
    std::cout << "📊 Их количество: " << количество_букв << "\n\n";

    // 5. Итоговое сообщение — философия современного C++
    std::cout << "💡 Современный C++ — это:\n"
              << "   • Безопасность (никаких утечек памяти),\n"
              << "   • Ясность (код читается как книга),\n"
              << "   • Эффективность (платишь только за то, что используешь).\n";

    return 0;
}
