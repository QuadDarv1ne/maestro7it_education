# CLI команды Simple HR

## Обзор

Simple HR предоставляет набор CLI команд для управления приложением через терминал.

## Использование

Все команды запускаются через Flask CLI:

```bash
flask cli <command>
```

## Доступные команды

### Инициализация базы данных

```bash
# Создать все таблицы
flask cli init-db

# Удалить все таблицы (с подтверждением)
flask cli drop-db
```

### Управление пользователями

```bash
# Создать нового пользователя (интерактивно)
flask cli create-user

# Создать пользователя с параметрами
flask cli create-user --username=admin --email=admin@example.com --role=admin

# Сбросить пароль пользователя
flask cli reset-password --username=admin

# Список всех пользователей
flask cli list-users
```

**Пример вывода list-users:**
```
Список пользователей:
------------------------------------------------------------
ID    Имя                  Email                     Роль      
------------------------------------------------------------
1     admin                admin@example.com         admin     
2     hr                   hr@example.com            hr        
------------------------------------------------------------
Всего: 2 пользователей
```

### Заполнение тестовыми данными

```bash
# Заполнить БД тестовыми данными
flask cli seed-db
```

Эта команда создаст:
- 2 пользователей (admin/admin, hr/hr)
- 5 отделов (IT, HR, Финансы, Маркетинг, Продажи)
- 5 должностей (Разработчик, Менеджер, Аналитик, Специалист, Директор)

### Статистика

```bash
# Показать статистику системы
flask cli stats
```

**Пример вывода:**
```
📊 Статистика системы Simple HR
==================================================
Сотрудников всего: 15
Активных сотрудников: 12
Отделов: 5
Должностей: 5
Пользователей: 2
==================================================
```

### Резервное копирование

```bash
# Создать резервную копию БД
flask cli backup-db
```

**Пример вывода:**
```
✓ Резервная копия создана: backup_20241116_143022.sql
  Размер: 15432 bytes
  Время: 2024-11-16 14:30:22
```

## Примеры использования

### Полная инициализация нового проекта

```bash
# 1. Инициализировать БД
flask cli init-db

# 2. Заполнить тестовыми данными
flask cli seed-db

# 3. Создать администратора
flask cli create-user --username=superadmin --email=super@example.com --role=admin

# 4. Проверить статистику
flask cli stats
```

### Управление пользователями

```bash
# Создать HR пользователя
flask cli create-user
# Следуйте интерактивным подсказкам

# Список пользователей
flask cli list-users

# Сброс пароля
flask cli reset-password --username=admin
```

### Резервное копирование перед обновлением

```bash
# Создать backup
flask cli backup-db

# Применить миграции
flask db upgrade

# Проверить статистику
flask cli stats
```

## Миграции базы данных

### Flask-Migrate команды

```bash
# Инициализация миграций (только один раз)
flask db init

# Создание новой миграции
flask db migrate -m "Описание изменений"

# Применение миграций
flask db upgrade

# Откат последней миграции
flask db downgrade

# История миграций
flask db history

# Показать текущую версию
flask db current

# Показать SQL для миграции (без выполнения)
flask db upgrade --sql
```

### Примеры миграций

```bash
# Добавить новое поле в модель
# 1. Изменить models.py
# 2. Создать миграцию
flask db migrate -m "Add phone field to Employee"

# 3. Проверить файл миграции
# 4. Применить
flask db upgrade

# Откат при ошибке
flask db downgrade
```

## Автоматизация с Makefile

Если используете Makefile:

```bash
# Инициализация
make init-db
make seed-db

# Создать пользователя
make create-user

# Статистика
make stats

# Backup
make backup

# Миграции
make migrate-init      # flask db init
make migrate-create    # flask db migrate
make migrate           # flask db upgrade
```

## Советы

### Безопасность паролей

При создании пользователей через CLI используйте сильные пароли:
- Минимум 12 символов
- Комбинация букв, цифр, спецсимволов
- Не используйте словарные слова

### Регулярные backup

Настройте cron для автоматического backup:

```bash
# Добавить в crontab
0 2 * * * cd /path/to/simple_hr && flask cli backup-db
```

### Миграции в production

Всегда создавайте backup перед миграциями:

```bash
flask cli backup-db && flask db upgrade
```

### Проверка перед удалением

Команда `drop-db` запрашивает подтверждение:

```bash
flask cli drop-db
# Вы уверены? Это удалит ВСЕ данные! [y/N]:
```

## Интеграция с Docker

```bash
# Выполнить команду в Docker контейнере
docker-compose exec web flask cli stats

# Создать пользователя в Docker
docker-compose exec web flask cli create-user

# Backup в Docker
docker-compose exec web flask cli backup-db
```

## Troubleshooting

### Ошибка "No such command"

Убедитесь, что команды зарегистрированы:
```bash
flask --help
# Должна быть группа команд 'cli'
```

### Ошибка подключения к БД

Проверьте переменные окружения:
```bash
echo $MYSQL_HOST
echo $MYSQL_USER
echo $MYSQL_DB
```

### Ошибка при миграции

Проверьте текущую версию:
```bash
flask db current
flask db history
```

Откатите проблемную миграцию:
```bash
flask db downgrade
```

## Расширенные примеры

### Скрипт полной настройки

```bash
#!/bin/bash

# setup.sh - Полная настройка Simple HR

set -e

echo "🚀 Настройка Simple HR..."

# Установка зависимостей
echo "📦 Установка зависимостей..."
pip install -r requirements.txt

# Инициализация БД
echo "🗄️ Инициализация БД..."
flask cli init-db

# Миграции
echo "📋 Применение миграций..."
flask db upgrade

# Тестовые данные
echo "🌱 Заполнение тестовыми данными..."
flask cli seed-db

# Создание администратора
echo "👤 Создание администратора..."
flask cli create-user --username=admin --email=admin@company.com --role=admin

# Статистика
echo "📊 Статистика:"
flask cli stats

echo "✅ Настройка завершена!"
```

Запуск:
```bash
chmod +x setup.sh
./setup.sh
```

---

Для получения полного списка всех команд:

```bash
flask --help
flask cli --help
flask db --help
```
