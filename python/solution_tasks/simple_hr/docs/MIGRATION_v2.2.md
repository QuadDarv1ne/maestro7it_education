# 🔄 Миграция на Simple HR v2.2

## 📋 Быстрый старт

### 1. Обновление кода

```bash
git pull origin main
```

### 2. Миграция базы данных

#### Автоматическая миграция (рекомендуется)

```bash
flask db-update
```

#### Ручная миграция

```bash
# Создать миграцию
flask db migrate -m "Add employee fields: birth_date, phone, address, salary"

# Применить миграцию
flask db upgrade
```

### 3. SQL скрипт (для ручного применения)

Если автоматическая миграция не работает:

```sql
-- Добавляем новые поля в таблицу employee
ALTER TABLE employee ADD COLUMN birth_date DATE;
ALTER TABLE employee ADD COLUMN phone VARCHAR(20);
ALTER TABLE employee ADD COLUMN address VARCHAR(200);
ALTER TABLE employee ADD COLUMN salary DECIMAL(10, 2);
ALTER TABLE employee ADD COLUMN created_at DATETIME DEFAULT CURRENT_TIMESTAMP;

-- Создаем индекс для быстрого поиска по дате рождения
CREATE INDEX idx_employee_birth_date ON employee(birth_date);
```

### 4. Проверка

```bash
# Проверить подключение к БД
flask shell

>>> from app.models import Employee
>>> Employee.query.first()
>>> # Должно работать без ошибок
```

---

## 🧪 Тестирование новых функций

### Запуск примеров

```bash
cd examples
python usage_examples.py
```

Вы увидите демонстрацию всех новых функций:
- ✅ Работа с праздниками
- ✅ Расчет рабочих дней
- ✅ Валидация данных
- ✅ Расчет отпусков
- ✅ Форматирование дат

---

## 📦 Новые зависимости

Никаких дополнительных зависимостей не требуется! Все работает на стандартной библиотеке Python.

---

## ⚙️ Настройка

### Включение расширенного логирования

В `config.py` или `.env`:

```python
# Логирование всех HTTP запросов
ENABLE_REQUEST_LOGGING = True

# Логирование SQL запросов
ENABLE_SQL_LOGGING = True
```

### Настройка Rate Limiting

```python
# В production автоматически включается ограничение:
# 100 запросов в минуту на IP адрес
```

---

## 🔧 Обновление существующих данных

### Заполнение дат рождения (опционально)

```python
from app import db
from app.models import Employee
from datetime import date

# Пример: добавить даты рождения для тестовых сотрудников
employees = Employee.query.all()
for emp in employees:
    if not emp.birth_date:
        # Генерируем случайную дату (для демо)
        emp.birth_date = date(1985, 1, 1)

db.session.commit()
```

---

## 📚 Использование новых функций

### В роутах

```python
from app.utils.date_utils import (
    format_date_russian,
    count_working_days,
    VacationCalculator
)

@app.route('/vacations/calculate')
def calculate_vacation():
    employee = Employee.query.get(id)
    earned_days = VacationCalculator.calculate_vacation_days_earned(
        employee.hire_date
    )
    return jsonify({'earned_days': earned_days})
```

### В моделях

```python
# Проверка дня рождения
if employee.is_birthday_soon(7):
    send_birthday_reminder(employee)

# Расчет стажа
years = employee.get_years_of_service()
```

### В валидации

```python
from app.utils.validators import EmployeeValidator, ValidationError

try:
    EmployeeValidator.validate_create(form_data)
    # Создаем сотрудника
except ValidationError as e:
    flash(str(e), 'error')
```

---

## 🐛 Возможные проблемы

### "Column already exists"

Если колонка уже существует:

```bash
# Откатить миграцию
flask db downgrade

# Удалить миграцию
rm migrations/versions/xxxx_*.py

# Создать заново
flask db migrate
flask db upgrade
```

### Проблемы с SQLite

SQLite не поддерживает `ALTER TABLE DROP COLUMN`. Если нужно удалить колонку:

1. Создайте новую таблицу с правильной схемой
2. Скопируйте данные
3. Удалите старую таблицу
4. Переименуйте новую

Или используйте PostgreSQL/MySQL в production.

---

## ✅ Checklist после миграции

- [ ] База данных обновлена
- [ ] Новые индексы созданы
- [ ] Приложение запускается без ошибок
- [ ] Dashboard отображается корректно
- [ ] Middleware работает (проверить логи)
- [ ] Валидация работает (тест формы создания сотрудника)
- [ ] Расчет отпусков работает
- [ ] Примеры выполняются без ошибок

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `logs/simple_hr.log`
2. Откройте issue на GitHub
3. Напишите автору

---

## 🎉 Готово!

После успешной миграции вы можете использовать все новые функции Simple HR v2.2!

**Следующие шаги:**
- Заполните даты рождения сотрудников
- Настройте уведомления о днях рождения
- Используйте новые валидаторы
- Мониторьте производительность через middleware

---

**Версия:** 2.2.0  
**Дата:** 18 ноября 2025 г.  
**Автор:** Дуплей Максим Игоревич
