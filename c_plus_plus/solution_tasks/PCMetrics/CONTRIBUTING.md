# Руководство по разработке PCMetrics

## Введение

Спасибо за интерес к проекту PCMetrics! Это руководство поможет вам начать работу над проектом.

## Требования

### Обязательные

- **Компилятор C++**: Visual Studio 2017+, GCC 7+, или Clang 5+
- **CMake**: версия 3.10 или выше
- **Windows SDK**: для доступа к PDH (Performance Data Helper)

### Опциональные (для GPU мониторинга)

- **NVIDIA NVML SDK**: для мониторинга NVIDIA GPU
- **AMD ADL SDK**: для мониторинга AMD GPU  
- **Intel GPA**: для мониторинга Intel GPU

## Структура проекта

```
PCMetrics/
├── include/          # Заголовочные файлы (.h)
├── src/              # Исходные файлы (.cpp)
├── tests/            # Тестовые файлы
├── libs/             # Внешние библиотеки (NVML, ADL, Intel GPA)
├── docs/             # Документация
└── build/            # Директория сборки (генерируется)
```

## Сборка проекта

### Базовая сборка

```powershell
# Создать директорию сборки
mkdir build
cd build

# Сгенерировать файлы проекта
cmake ..

# Собрать проект
cmake --build .
```

### Сборка с поддержкой GPU

```powershell
# NVIDIA NVML
cmake -DENABLE_NVML=ON ..

# AMD ADL
cmake -DENABLE_ADL=ON ..

# Intel GPA
cmake -DENABLE_INTEL_GPA=ON ..

# Все GPU библиотеки
cmake -DENABLE_NVML=ON -DENABLE_ADL=ON -DENABLE_INTEL_GPA=ON ..
```

## Запуск тестов

```powershell
cd build/bin
.\pcmetrics_test.exe
```

## Стиль кода

### Соглашения об именовании

- **Классы**: PascalCase (например, `CPUMonitor`)
- **Функции**: camelCase (например, `getCPUUsage`)
- **Переменные**: camelCase (например, `cpuUsage`)
- **Константы**: UPPER_SNAKE_CASE (например, `MAX_RETRIES`)
- **Приватные члены**: camelCase с префиксом `_` не требуется

### Форматирование

- Отступы: 4 пробела (не табуляция)
- Максимальная длина строки: 120 символов
- Открывающая фигурная скобка на той же строке
- Использование умных указателей где возможно

### Пример

```cpp
class ExampleClass {
private:
    int privateVariable;
    
public:
    ExampleClass() : privateVariable(0) {}
    
    void performAction(int parameter) {
        if (parameter > 0) {
            // Действие
        }
    }
};
```

## Документация

Используйте Doxygen для документирования кода:

```cpp
/**
 * @brief Краткое описание функции
 * 
 * Детальное описание того, что делает функция.
 * 
 * @param paramName Описание параметра
 * @return Описание возвращаемого значения
 */
int exampleFunction(int paramName) {
    return paramName * 2;
}
```

## Логирование

Всегда используйте класс Logger для вывода отладочной информации:

```cpp
#include "logger.h"

Logger::getInstance().info("Информационное сообщение");
Logger::getInstance().warning("Предупреждение");
Logger::getInstance().error("Ошибка");
Logger::getInstance().debug("Отладочная информация");
```

## Обработка ошибок

- Всегда проверяйте возвращаемые значения
- Используйте логирование для записи ошибок
- Возвращайте bool для индикации успеха/неудачи
- Используйте константы для кодов ошибок

## Добавление новых функций

1. Создайте заголовочный файл в `include/`
2. Создайте файл реализации в `src/`
3. Добавьте соответствующие тесты в `tests/`
4. Обновите CMakeLists.txt если необходимо
5. Обновите README.md с описанием новой функции

## Pull Request процесс

1. Создайте форк проекта
2. Создайте feature ветку (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Требования к Pull Request

- Код должен компилироваться без ошибок
- Все тесты должны проходить
- Добавьте тесты для новой функциональности
- Обновите документацию
- Следуйте стилю кода проекта

## Лицензия

Проект распространяется под лицензией MIT. См. файл LICENSE для деталей.

## Контакты

Если у вас есть вопросы, создайте Issue в репозитории проекта.

---

Спасибо за вклад в развитие PCMetrics! 🚀
