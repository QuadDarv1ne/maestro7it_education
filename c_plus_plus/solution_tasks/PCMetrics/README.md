# PCMetrics

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![Platform](https://img.shields.io/badge/platform-Windows-lightgrey)
![Language](https://img.shields.io/badge/language-C++-orange)
![License](https://img.shields.io/badge/license-MIT-green)

**PCMetrics** — мощный инструмент для мониторинга системных ресурсов компьютера в реальном времени.

Отслеживайте загрузку CPU, состояние памяти, использование дисков и получайте детальную информацию о железе вашего ПК.

## 🚀 Возможности

### 🖥️ Мониторинг CPU

- Реальная загрузка процессора в процентах
- Определение количества логических процессоров
- Информация об архитектуре (`x86`, `x64`, `ARM`)
- Непрерывный мониторинг с настраиваемым интервалом
- Детальная информация о процессоре (название, частота, кэш)

### 💾 Анализ дисков

- Сканирование всех подключенных дисков
- Общий и свободный объем для каждого диска
- Процент использования дискового пространства
- Определение типа диска (жесткий/съемный)
- Мониторинг скорости чтения/записи
- Информация о метке тома и файловой системе

### 🧠 Мониторинг памяти

- Общий объем физической памяти (`RAM`)
- Доступная память в реальном времени
- Процент использования памяти
- Информация о виртуальной памяти
- Информация о файле подкачки
- Время непрерывной работы системы

### 🌐 Сетевой мониторинг

- Информация о всех сетевых интерфейсах
- Статистика трафика (принято/отправлено)
- Скорость интерфейса
- Форматирование данных в удобочитаемом виде

### 🎮 GPU мониторинг (расширяемая функция)

- Поддержка `NVIDIA GPU` через `NVML`
- Поддержка `AMD GPU` через `ADL SDK`
- Поддержка `Intel GPU`
- Мониторинг температуры, загрузки и использования памяти

### 📊 Экспорт данных

- Экспорт метрик в формат `CSV`
- Экспорт метрик в формат `JSON`
- Поддержка автоматического создания временных меток
- Полная информация о всех системных компонентах

### 🔄 Непрерывный мониторинг

- Режим реального времени с автообновлением
- Пауза/возобновление мониторинга
- Выход по нажатию клавиши
- Настраиваемая частота обновления
- Компактный и детальный режимы отображения

### 📝 Логирование

- Комплексное логирование всех событий приложения
- Различные уровни логирования (`DEBUG`, `INFO`, `WARNING`, `ERROR`)
- Запись логов в файл и вывод в консоль
- Временные метки для всех событий

## 📋 Требования

- **ОС:** `Windows 7/8/10/11`
- **Компилятор:** `Visual Studio 2017+` или `MinGW-w64`
- **Библиотеки:** `PDH (Performance Data Helper)` - входит в `Windows SDK`

### Опциональные зависимости для GPU:

- `NVIDIA NVML` (для мониторинга NVIDIA GPU)
- `AMD ADL SDK` (для мониторинга AMD GPU)
- `Intel Graphics API` (для мониторинга Intel GPU)

## 📁 Структура библиотек

Проект теперь включает директорию `libs/` для размещения дополнительных библиотек и драйверов:

```
PCMetrics/
├── libs/
│   ├── nvml/     # NVIDIA Management Library
│   ├── adl/      # AMD Display Library
│   └── intel/    # Intel Graphics Performance Analyzers
```

## 🛠️ Компиляция

### Visual Studio

```bash
cl /EHsc /I include src\main.cpp pdh.lib /Fe:pcmetrics.exe
```

Или откройте `.cpp` файл в Visual Studio и нажмите `Ctrl+F5`

### MinGW / GCC

```bash
g++ -I include src/main.cpp -o pcmetrics.exe -lpdh -std=c++17
```

> ⚠️ **Важно**: При ручной компиляции убедитесь, что включили все файлы `.cpp` из директории `src/`. Если вы пропустите какие-либо файлы, вы получите ошибки "undefined reference".

### CMake (рекомендуется)

```bash
mkdir build
cd build
cmake ..
cmake --build .
```

> ✅ **Рекомендация**: Используйте CMake для сборки проекта, так как он автоматически находит и включает все необходимые файлы.

### Автоматические скрипты сборки

Для еще более простой сборки проекта вы можете использовать готовые скрипты:

**Для Windows:**
```bash
build_project.bat
```

**Для Linux/macOS:**
```bash
chmod +x build_project.sh
./build_project.sh
```

### Подробные руководства

Для более подробной информации о сборке и запуске проекта смотрите:
- [Руководство по сборке (русский)](docs/BUILD_GUIDE.md)
- [Build Guide (English)](docs/BUILD_GUIDE_EN.md)

### Сборка с поддержкой NVIDIA NVML

```bash
mkdir build
cd build
cmake -DENABLE_NVML=ON ..
cmake --build .
```

### Сборка с поддержкой AMD ADL

```bash
mkdir build
cd build
cmake -DENABLE_ADL=ON ..
cmake --build .
```

### Сборка с поддержкой Intel GPA

```bash
mkdir build
cd build
cmake -DENABLE_INTEL_GPA=ON ..
cmake --build .
```

### Сборка со всеми GPU библиотеками

```bash
mkdir build
cd build
cmake -DENABLE_NVML=ON -DENABLE_ADL=ON -DENABLE_INTEL_GPA=ON ..
cmake --build .
```

## 🎯 Использование

### Базовый запуск

```bash
pcmetrics.exe
```

**Программа выведет:**

- Информацию о процессоре
- Загрузку CPU в течение 5 секунд
- Состояние памяти
- Детальную информацию о всех дисках
- Информацию о сетевых интерфейсах
- Информацию о GPU (если доступна поддержка)

### Пример вывода

```bash
=== МОНИТИРОВАНИЕ СИСТЕМНЫХ РЕСУРСОВ ===

=== Информация о процессоре ===
Количество процессоров: 8
Архитектура: x64 (AMD or Intel)

Мониторинг загрузки CPU (5 секунд)...
CPU загрузка: 15.23%
CPU загрузка: 18.45%
CPU загрузка: 12.67%

=== Информация о памяти ===
Всего физической памяти: 16 ГБ
Доступно физической памяти: 8 ГБ
Использование памяти: 50%

=== Информация о дисках ===
Диск: C:\
Тип: Жесткий диск
Всего: 500 ГБ
Свободно: 150 ГБ
Занято: 350 ГБ
Использовано: 70.00%
```

## 📁 Структура проекта

```bash
PCMetrics/
│
├── src/                      # Исходные файлы (.cpp)
│   ├── main.cpp              # Основной файл программы
│   ├── cpu_monitor.cpp       # Реализация мониторинга CPU
│   ├── disk_monitor.cpp      # Реализация мониторинга дисков
│   ├── memory_monitor.cpp    # Реализация мониторинга памяти
│   ├── gpu_monitor.cpp       # Реализация мониторинга GPU
│   ├── network_monitor.cpp   # Реализация мониторинга сети
│   ├── metrics_exporter.cpp  # Экспорт метрик в различные форматы
│   └── logger.cpp            # Реализация логирования
│
├── include/                  # Заголовочные файлы (.h)
│   ├── cpu_monitor.h         # Заголовок для мониторинга CPU
│   ├── disk_monitor.h        # Заголовок для мониторинга дисков
│   ├── memory_monitor.h      # Заголовок для мониторинга памяти
│   ├── gpu_monitor.h         # Заголовок для мониторинга GPU
│   ├── network_monitor.h     # Заголовок для мониторинга сети
│   ├── metrics_exporter.h    # Заголовок для экспорта метрик
│   └── logger.h              # Заголовок для логирования
│
├── libs/                     # Дополнительные библиотеки и драйверы
│   ├── nvml/                 # NVIDIA Management Library
│   ├── adl/                  # AMD Display Library
│   └── intel/                # Intel Graphics Performance Analyzers
│
├── tests/                    # Тестовые файлы
├── build/                    # Директория для сборки
├── docs/                     # Документация
│   ├── BUILD_GUIDE.md        # Руководство по сборке (русский)
│   └── BUILD_GUIDE_EN.md     # Build Guide (English)
├── CMakeLists.txt            # Конфигурация CMake
├── README.md                 # Этот файл
└── LICENSE                   # Лицензия MIT
```

## 🧪 Тестирование

Проект включает в себя тесты для проверки корректности работы всех компонентов:

```bash
cd build/bin
.\pcmetrics_test.exe
```

Также доступен расширенный тестовый набор:
```bash
cd build/bin
.\comprehensive_test.exe
```

## 🎮 Управление в режиме непрерывного мониторинга

После запуска программы вы можете выбрать режим непрерывного мониторинга:

| Клавиша | Действие |
|---------|----------|
| q/Q | Выход из режима мониторинга |
| Пробел | Пауза/возобновление мониторинга |
| d/D | Переключение между компактным и детальным режимами |
| + | Увеличение интервала обновления |
| - | Уменьшение интервала обновления |

### Экспорт метрик

После запуска программы вы можете выбрать опцию экспорта метрик:
1. Выберите формат (CSV или JSON)
2. Укажите имя файла
3. Метрики будут сохранены в указанном файле

### Непрерывный мониторинг

После основного отчета программа предложит перейти в режим непрерывного мониторинга:
- Нажмите `q` или `Q` для выхода
- Нажмите любую другую клавишу для паузы/возобновления

### Логирование

Приложение автоматически создает файл логов `pcmetrics.log` в директории запуска:
- Все события приложения записываются в лог
- Поддерживаются различные уровни логирования
- Логи содержат временные метки для каждого события
- Логи выводятся как в файл, так и в консоль

## 🤝 Вклад в проект

**Чтобы внести свой вклад:**

1. Сделайте `Fork` проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте `Pull Request`

## 📄 Лицензия

Этот проект лицензирован по лицензии MIT - см. файл [LICENSE](LICENSE) для подробностей.