/*
    🌟 Программа - Поиск экстремумов 🌟

    📖 Описание:
    
    Эта программа помогает найти максимальное и минимальное число в последовательности,
    введенной пользователем. Она идеально подходит для анализа данных, поиска рекордов
    или просто для того, чтобы удивить друзей своими навыками программирования.

    📝 Как это работает:
    1. Вы вводите числа одно за другим.
    2. Когда захотите остановиться, введите -1.
    3. Программа мгновенно определит самое большое и самое маленькое число в вашей последовательности.

    📝 Пример использования:
    Введите последовательность чисел (для завершения введите ';'):
    10
    3
    7
    42
    15
    -1
    Максимальное число: 42
    Минимальное число: 3

    Автор: Дуплей М.И.
    Версия: 1.0
    Дата: 09-03-2025
*/

#include <iostream>
#include <sstream> // для std::istringstream
#include <vector>
#include <climits> // для INT_MIN и INT_MAX
using namespace std;

int main() {
    string input;
    vector<int> numbers;

    cout << "Введите последовательность чисел (для завершения введите ';'):" << endl;

    // Считываем всю строку ввода
    getline(cin, input, ';');

    // Используем stringstream для разбиения строки на числа
    istringstream stream(input);
    int number;
    while (stream >> number) {
        numbers.push_back(number);
    }

    // Если числа не были введены
    if (numbers.empty()) {
        cout << "Вы не ввели ни одного числа." << endl;
        return 0;
    }

    // Поиск максимального и минимального числа
    int maxNumber = INT_MIN;
    int minNumber = INT_MAX;

    for (int num : numbers) {
        if (num > maxNumber) {
            maxNumber = num;
        }
        if (num < minNumber) {
            minNumber = num;
        }
    }

    // Вывод результатов
    cout << "Максимальное число: " << maxNumber << endl;
    cout << "Минимальное число: " << minNumber << endl;

    return 0;
}
