'''
Задание 1: best_weather.py
Автор: Дуплей Максим Игоревич / Дарья Руслановна Оборотова

Задача:
Среди введённых строк (предсказаний погоды) найти ту, которая содержит заданное слово
и является наименьшей в лексикографическом порядке.

Пример ввода:
дождь
Завтра будет дождь
Сегодня солнечно
Ожидается дождь ночью
Пасмурно, но без дождя

| --------------------------------------------------------------------------------------------------------------------------- |
| Программа должна найти все строки, содержащие слово дождь, и выбрать из них лексикографически наименьшую.                   |
| В данном случае это: Завтра будет дождь (так как 'З' < 'О' в кодировке UTF-8/Unicode, и русская буква «З» идёт раньше «О»). |
| --------------------------------------------------------------------------------------------------------------------------- |
'''

# считываем искомое слово
target_word = input().strip()

min_len = None

# читаем строки до пустой
while True:
    line = input().strip()
    if line == "":
        break
    # проверяем наличие целевого слова в строке
    if target_word in line:
        # обновляем минимальную строку, если нужно
        if min_len is None or line < min_len:
            min_len = line
    
# выводим результат
if min_len is not None:
    print(min_len)