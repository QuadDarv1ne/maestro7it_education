# Chess Engine Test Suite

Комплексный набор тестов для проверки корректности работы шахматного движка.

## 📋 Структура тестов

### C++ Тесты
- **`test_game_states.cpp`** - Базовые тесты состояний игры (шах, мат, пат)
- **`test_special_moves.cpp`** - Тесты специальных ходов (рокировка, взятие на проходе, превращение)
- **`enhanced_game_tests.cpp`** - Расширенные тесты с метриками производительности
- **`comprehensive_special_moves.cpp`** - Комплексные тесты специальных ходов с edge cases

### Python Тесты
- **`test_fastapi.py`** - Базовое тестирование FastAPI endpoints
- **`enhanced_fastapi_test.py`** - Расширенное тестирование API с нагрузочными тестами

### Утилиты
- **`run_all_tests.py`** - Скрипт для запуска всех тестов и генерации отчетов

## 🚀 Запуск тестов

### Запуск всех тестов
```bash
cd tests
python run_all_tests.py
```

### Запуск отдельных тестов

#### C++ тесты
```bash
# Компиляция и запуск
g++ -std=c++17 -I../include test_game_states.cpp -o game_tests
./game_tests

# Или с оптимизацией
g++ -std=c++17 -O2 -I../include enhanced_game_tests.cpp -o enhanced_tests
./enhanced_tests
```

#### Python тесты
```bash
# Базовые FastAPI тесты
python test_fastapi.py

# Расширенные тесты (требуется запущенный сервер)
python enhanced_fastapi_test.py
```

## 📊 Что тестируется

### Game State Tests
- ✅ Обнаружение шаха различными фигурами
- ✅ Обнаружение мата (включая классические паттерны)
- ✅ Обнаружение пата
- ✅ Условия ничьей (тройное повторение, 50 ходов, недостаток материала)

### Special Moves Tests
- ✅ Рокировка (королевский и ферзевый фланг)
- ✅ Взятие на проходе
- ✅ Превращение пешки (все 4 варианта)
- ✅ Edge cases (блокировка, шах во время рокировки)

### API Tests
- ✅ Health check endpoint
- ✅ Создание игры
- ✅ Выполнение ходов
- ✅ Ходы ИИ
- ✅ Отмена ходов
- ✅ Обработка ошибок
- ✅ CORS заголовки
- ✅ Rate limiting

## 📈 Метрики производительности

Расширенные тесты собирают метрики:
- Время выполнения каждого теста
- Среднее время генерации ходов
- Успешность операций
- Потребление памяти (косвенно)

## 📁 Структура отчетов

После запуска тестов создаются файлы:
- `test_reports/test_report_YYYYMMDD_HHMMSS.json` - Детальный отчет
- `enhanced_api_test_report.json` - Отчет расширенных API тестов
- `api_documentation.json` - Документация API

## 🛠️ Разработка новых тестов

### Шаблон C++ теста
```cpp
#include "../include/board.hpp"
#include <iostream>
#include <cassert>

void testMyFeature() {
    std::cout << "Testing my feature..." << std::endl;
    
    Board board;
    // Setup test position
    board.setupStartPosition();
    
    // Execute test
    // ...
    
    // Verify results
    assert(condition);
    std::cout << "✓ Test passed!" << std::endl;
}
```

### Шаблон Python теста
```python
def test_my_feature():
    """Test my feature functionality"""
    # Setup
    # ...
    
    # Execute
    result = my_function()
    
    # Verify
    assert result == expected_value
    return {"status": "passed"}
```

## ⚠️ Известные ограничения

1. Для API тестов должен быть запущен FastAPI сервер
2. Некоторые C++ тесты требуют наличия всех заголовочных файлов
3. Тесты производительности могут варьироваться в зависимости от системы

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что сервер запущен для API тестов
3. Проверьте пути к заголовочным файлам для C++ тестов