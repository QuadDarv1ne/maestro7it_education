# ♟ Chess Engine

> **Статус проекта**: 🔧 В разработке - Базовая структура создана, реализация в процессе

Чистый и модульный шахматный движок, написанный на `C++`

Этот проект представляет собой реализацию классических шахмат с возможностью игры человека против компьютера или двух игроков друг против друга в терминале.

## ✨ Особенности

- **Полноценные правила**: Поддержка всех стандартных ходов, включая рокировку, взятие на проходе и превращение пешки.
- **Терминальный интерфейс**: Простой и кросс-платформенный интерфейс для командной строки.
- **Искусственный интеллект**: Реализация алгоритма минимакс с альфа-бета отсечением для принятия решений компьютерным соперником.
- **Модульная архитектура**: Чистое разделение логики игры, пользовательского интерфейса и алгоритма ИИ для простоты понимания и расширения.
- **Кроссплатформенность**: Проект может быть собран и запущен на `Windows`, `Linux` и `macOS`

## 📁 Структура проекта

```printtext
chess_engine/
├── src/
│   ├── board/          # Модуль представления шахматной доски и фигур
│   ├── logic/          # Проверка правил и генерация ходов
│   ├── engine/         # Алгоритмы ИИ (минимакс, оценка позиции)
│   ├── ui/             # Терминальный интерфейс (ввод/вывод, отрисовка доски)
│   └── main.cpp        # Точка входа в программу
├── include/            # Публичные заголовочные файлы
├── tests/              # Модульные тесты (опционально)
├── CMakeLists.txt      # Конфигурация для системы сборки CMake
└── README.md           # Эта документация
```

## 🛠 Сборка и запуск

Для сборки проекта необходим компилятор C++17 (или новее) и система сборки CMake (версия 3.10+).

### Windows

```cmd
# Используйте готовый скрипт сборки
build.bat
```

Или вручную:

```cmd
mkdir build && cd build
cmake .. -G "MinGW Makefiles"
mingw32-make
```

### Linux/macOS

```bash
# Используйте готовый скрипт сборки
chmod +x build.sh
./build.sh
```

Или вручную:

```bash
mkdir build && cd build
cmake ..
make
```

После успешной сборки в папке `build` появится исполняемый файл:
- Windows: `chess_engine.exe`
- Linux/macOS: `chess_engine`

### Запуск

```bash
# Windows
cd build
chess_engine.exe

# Linux/macOS
cd build
./chess_engine
```

## 🎮 Использование

**При запуске программы откроется главное меню с вариантами:**

- Играть против компьютера
- Играть против другого игрока (на одном компьютере)
- Настройки (уровень сложности, цвет фигур)
- Выйти

### Управление в игре:

- Ходы вводятся в алгебраической нотации (например, `e2-e4` для хода пешки с e2 на e4) или в сокращенной форме (например, `Nf3` для хода коня на f3).
- Для выхода из игры или возврата в меню используйте команду `quit` или `menu`.

## ## Алгоритм ИИ

Движок компьютера использует классический алгоритм минимакс с альфа-бета отсечением для поиска лучшего хода на заданную глубину.

**Силу игры можно регулировать в настройках, изменяя:**

- **Глубина поиска**: количество полуходов, на которое "думает" ИИ.
- **Функция оценки позиции**: которая учитывает материальный баланс, активность фигур, контроль центра и безопасность короля.

## 🤝 Вклад в проект

**Если вы хотите улучшить этот проект, ваши `Pull Requests` приветствуются:**

1. Сделайте форк репозитория.
2. Создайте ветку для своей функции (`git checkout -b feature/amazing-feature`).
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`).
4. Отправьте изменения в свой форк (`git push origin feature/amazing-feature`).
5. Откройте `Pull Request` в основном репозитории.

Пожалуйста, убедитесь, что ваш код соответствует существующему стилю, и по возможности добавляйте тесты.

## 📄 Лицензия

Этот проект распространяется под лицензией `MIT`

Подробности смотрите в файле `LICENSE`

## 📧 Контакты

