# Chess Calendar RU

Шахматный календарь России - приложение для отслеживания шахматных турниров в России.

## 📋 Описание проекта

Chess Calendar RU - это веб-приложение на основе Flask с микросервисной архитектурой, которое собирает информацию о шахматных турнирах в России с официальных источников (FIDE и Российская Федерация Шахмат), предоставляет удобный интерфейс для просмотра, фильтрации и поиска турниров, а также включает админ-панель для управления данными.

## 🚀 Последние обновления (2026-02-17)

Проект прошел масштабную оптимизацию:
- ✅ **Унифицирована система кэширования** - объединены 3 модуля в один (`unified_cache.py`)
- ✅ **Унифицирована система резервного копирования** - объединены 2 модуля в один (`unified_backup.py`)
- ✅ **Удалено 13 дублирующихся и неиспользуемых файлов**
- ✅ **Сокращена кодовая база на ~2200 строк (-73%)**
- ✅ **Улучшена производительность и поддерживаемость**

Подробнее: [OPTIMIZATION_CHANGELOG.md](OPTIMIZATION_CHANGELOG.md)

## 🏗️ Архитектура

Проект использует современную микросервисную архитектуру:

- **API Gateway** - Единая точка входа с JWT аутентификацией
- **Микросервисы** - Независимые сервисы для турниров, пользователей, уведомлений и т.д.
- **Celery** - Асинхронная обработка задач (парсинг, уведомления, аналитика)
- **Redis** - Многоуровневое кэширование и брокер сообщений
- **Docker** - Контейнеризация всех компонентов

Подробнее: [ARCHITECTURE.md](ARCHITECTURE.md)

## ✨ Основные возможности

### Функциональные возможности
- **Автоматический парсинг турниров**: Асинхронное получение информации о турнирах с FIDE и Российской Федерации Шахмат через Celery
- **Интерактивный календарь**: Удобное отображение турниров с возможностью фильтрации по различным критериям
- **Админ-панель**: Защищенная панель управления с JWT аутентификацией для добавления, редактирования и удаления турниров
- **PWA поддержка**: Возможность установки как приложения на мобильные устройства
- **Персонализированные рекомендации**: Интеллектуальная система рекомендаций на основе предпочтений пользователей
- **Уведомления**: Асинхронная система уведомлений о новых турнирах и изменениях
- **API**: REST API с JWT аутентификацией для интеграции с другими системами
- **Экспорт данных**: Возможность экспорта турниров в CSV и JSON форматы

### Архитектурные возможности
- **API Gateway**: Единая точка входа с маршрутизацией, аутентификацией и rate limiting
- **JWT аутентификация**: Stateless аутентификация с токенами и refresh механизмом
- **Многоуровневое кэширование**: L1 (In-Memory) + L2 (Redis) + L3 (CDN) с автоматической инвалидацией
- **Асинхронная обработка**: Celery для фоновых задач (парсинг, уведомления, аналитика)
- **Микросервисная архитектура**: Независимые сервисы с возможностью горизонтального масштабирования
- **Мониторинг**: Health checks, метрики, Flower для мониторинга Celery
- **Безопасность**: Rate limiting, CSRF защита, security headers, валидация данных
- **Аналитика**: Комплексная система аналитики для отслеживания использования и эффективности

## 🛠️ Технологии

### Backend
- **Python 3.11+**
- **Flask** - веб-фреймворк
- **Flask-SQLAlchemy** - ORM для работы с базой данных
- **Celery** - асинхронная обработка задач
- **Redis** - кэширование и брокер сообщений
- **PyJWT** - JWT аутентификация
- **Flask-Limiter** - rate limiting

### Микросервисы
- **API Gateway** - маршрутизация и аутентификация
- **Tournament Service** - управление турнирами
- **User Service** - управление пользователями
- **Parser Service** - парсинг турниров
- **Notification Service** - уведомления
- **Recommendation Service** - рекомендации
- **Favorites Service** - избранное
- **Calendar Service** - календари
- **Image Optimization Service** - оптимизация изображений
- **CDN Service** - кэширование статики

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Nginx** - reverse proxy и load balancer
- **SQLite/PostgreSQL** - база данных
- **Flower** - мониторинг Celery

### Frontend
- **Bootstrap 5** - UI фреймворк
- **JavaScript** - интерактивность
- **PWA** - Progressive Web App

### Парсинг и обработка
- **BeautifulSoup4** - парсинг HTML
- **Requests** - HTTP-запросы
- **Selenium** - динамический контент

## 📦 Установка и запуск

### Быстрый старт с Docker (рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd chess-calendar-ru

# 2. Создайте .env файл
cp .env.example .env
# Отредактируйте .env (минимум SECRET_KEY)

# 3. Запустите все сервисы
make start
# или
./scripts/start-all.sh  # Linux/Mac
scripts\start-all.bat   # Windows

# 4. Создайте администратора
make create-admin
```

Приложение будет доступно по адресу: http://localhost:5000

### Доступные команды (Makefile)

```bash
make help              # Показать все команды
make start             # Запустить сервисы
make stop              # Остановить сервисы
make logs              # Показать логи
make health            # Проверка здоровья
make backup            # Создать backup
make test              # Запустить тесты
make deploy            # Развернуть в production
make start-monitoring  # Запустить мониторинг
```

Полный список команд: `make help`

### Ручная установка

#### Требования

- Python 3.11+
- Redis
- Docker (опционально)

#### Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Настройка конфигурации

Создайте файл `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и установите минимум:
- `SECRET_KEY` - секретный ключ для Flask
- `DATABASE_URL` - URL базы данных
- `REDIS_URL` - URL Redis сервера

#### Запуск сервисов

```bash
# Запуск Redis
redis-server

# Запуск Celery Worker
celery -A app.celery_app worker --loglevel=info

# Запуск Celery Beat (планировщик)
celery -A app.celery_app beat --loglevel=info

# Запуск Flask приложения
python run.py
```

## 🔐 Административная панель

### Доступ через API Gateway

Для доступа к административной панели используйте JWT аутентификацию:

1. Получите JWT токен через `/auth/login`
2. Используйте токен в заголовке `Authorization: Bearer <token>`

### Создание администратора

```bash
# Через Docker
docker-compose exec api-gateway python manage.py --action create-admin \
  --username admin \
  --email admin@example.com \
  --password SecurePassword123

# Локально
python manage.py --action create-admin \
  --username admin \
  --email admin@example.com \
  --password SecurePassword123
```

### Использование API с JWT

```bash
# 1. Получение токена
curl -X POST http://localhost:5000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "SecurePassword123"}'

# 2. Использование токена
TOKEN="<your-token>"
curl -X GET http://localhost:5000/api/tournaments \
  -H "Authorization: Bearer $TOKEN"

# 3. Обновление токена
curl -X POST http://localhost:5000/auth/refresh \
  -H "Authorization: Bearer $TOKEN"
```

## 📊 Структура базы данных

### Таблица турниров

- `id` - уникальный идентификатор
- `name` - название турнира
- `start_date` - дата начала
- `end_date` - дата окончания
- `location` - место проведения
- `category` - категория (FIDE, National, Regional и т.д.)
- `status` - статус (Scheduled, Ongoing, Completed)
- `fide_id` - идентификатор турнира в системе FIDE
- `source_url` - URL источника
- `created_at` - дата создания
- `updated_at` - дата последнего обновления

### Таблица пользователей

- `id` - уникальный идентификатор
- `username` - имя пользователя
- `email` - email пользователя
- `password_hash` - хэш пароля
- `is_admin` - флаг администратора
- `created_at` - дата создания
- `last_login` - дата последнего входа
- `is_active` - статус активности
- `api_key` - API ключ для доступа к API

## 🚀 Автоматическое обновление

Приложение автоматически обновляет данные о турнирах через Celery:

### Периодические задачи

- **Парсинг FIDE турниров**: Каждые 6 часов
- **Парсинг CFR турниров**: Каждые 6 часов (со смещением 30 минут)
- **Отправка уведомлений**: Каждый час
- **Проверка предстоящих турниров**: Ежедневно в 9:00
- **Генерация аналитики**: Ежедневно в 3:00
- **Резервное копирование**: Ежедневно в 4:00
- **Очистка старых данных**: Еженедельно

### Ручное обновление

```bash
# Через API (требуется admin токен)
curl -X POST http://localhost:5000/api/admin/update \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# Через Celery напрямую
docker-compose exec celery-worker python -c "
from app.tasks.parser_tasks import parse_fide_tournaments
parse_fide_tournaments.delay()
"

# Через manage.py
python manage.py --action update-tournaments
```

### Мониторинг задач

Откройте Flower для мониторинга Celery: http://localhost:5555

## 📱 PWA возможности

Приложение поддерживает режим Progressive Web App (PWA):

- Возможность установки на мобильные устройства
- Офлайн режим (ограниченный)
- Push-уведомления (в разработке)
- Автономный режим работы

## 🔧 API

### Аутентификация

```bash
POST /auth/login          # Получение JWT токена
POST /auth/refresh        # Обновление токена
GET  /auth/verify         # Проверка токена
```

### Турниры

```bash
GET    /api/tournaments              # Получить все турниры (с кэшированием)
GET    /api/tournaments/<id>         # Получить турнир по ID
POST   /api/tournaments              # Создать турнир (admin)
PUT    /api/tournaments/<id>         # Обновить турнир (admin)
DELETE /api/tournaments/<id>         # Удалить турнир (admin)
```

### Пользователи

```bash
GET    /api/users                    # Получить всех пользователей (admin)
GET    /api/users/<id>               # Получить пользователя по ID
POST   /api/users                    # Регистрация нового пользователя
PUT    /api/users/<id>               # Обновить пользователя
```

### Рекомендации и избранное

```bash
GET    /api/recommendations          # Получить рекомендации (auth)
GET    /api/favorites                # Получить избранное (auth)
GET    /api/notifications            # Получить уведомления (auth)
```

### Система

```bash
GET    /health                       # Health check
GET    /metrics                      # Метрики (admin)
```

### Примеры использования

См. [QUICK_START.md](QUICK_START.md) для подробных примеров.

## 🔒 Безопасность

### Реализованные меры

- **JWT аутентификация**: Stateless токены с истечением срока действия
- **Rate limiting**: Защита от DDoS-атак (1000 req/day, 100 req/hour)
- **CSRF защита**: Защита от межсайтовой подделки запросов
- **Security headers**: CSP, HSTS, X-Frame-Options, X-Content-Type-Options
- **Валидация данных**: Проверка всех входных данных
- **Хэширование паролей**: Безопасное хранение паролей
- **API Gateway**: Централизованная точка контроля доступа

### Рекомендации для production

- Используйте HTTPS (SSL/TLS сертификаты)
- Смените `SECRET_KEY` на случайное значение
- Используйте PostgreSQL вместо SQLite
- Настройте firewall и ограничьте доступ к портам
- Регулярно обновляйте зависимости
- Настройте мониторинг и алерты
- Используйте сильные пароли для администраторов

## 📈 Мониторинг и логирование

### Доступные инструменты

- **Health Checks**: Проверка состояния всех сервисов (`/health`)
- **Flower**: Мониторинг Celery задач (http://localhost:5555)
- **Метрики**: API метрики через `/metrics` (admin only)
- **Логирование**: Структурированные логи с ротацией

### Проверка состояния системы

```bash
# Проверка всех сервисов
curl http://localhost:5000/health

# Метрики API Gateway
curl -H "Authorization: Bearer $ADMIN_TOKEN" \
  http://localhost:5000/metrics

# Статистика кэша
docker-compose exec api-gateway python -c "
from app.utils.unified_cache import cache
print(cache.get_stats())
"

# Статистика Celery
docker-compose exec celery-worker celery -A app.celery_app inspect stats
```

### Логи

```bash
# Все логи
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f api-gateway

# Последние 100 строк
docker-compose logs --tail=100 celery-worker
```

## 🛡️ Обработка ошибок

- Комплексная система обработки ошибок 404 и 500
- Логирование исключений
- Уведомления об ошибках администратору
- Автоматическая генерация отчетов об ошибках

Приложение поддерживает режим Progressive Web App (PWA):

- Возможность установки на мобильные устройства
- Офлайн режим (ограниченный)
- Push-уведомления (в разработке)
- Автономный режим работы

## 🔧 API

Приложение предоставляет REST API:

- `GET /api/tournaments` - получить все турниры
- `GET /api/tournaments/<id>` - получить турнир по ID
- `GET /admin/api/health` - проверка состояния системы
- `GET /admin/api/metrics` - получить метрики производительности

## 🔒 Безопасность

- Аутентификация администраторов с использованием сессий
- Хранение паролей с использованием безопасного хэширования
- Валидация входных данных
- Защита от SQL-инъекций через ORM

## 📈 Мониторинг и логирование

- Система логирования событий и ошибок
- Мониторинг производительности
- Проверка состояния системы (health checks)
- Статистика использования

## 🧪 Тестирование

Для запуска тестов:

```bash
# Все тесты
python -m pytest tests/

# С покрытием
python -m pytest tests/ --cov=app --cov-report=html

# Конкретный тест
python -m pytest tests/test_api.py
```

## 📚 Документация

- **[ARCHITECTURE.md](ARCHITECTURE.md)** - Подробная архитектура системы
- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт и примеры использования
- **[API Documentation](http://localhost:5000/api/docs)** - Swagger документация API

## 🔧 Полезные команды

### Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск конкретного сервиса
docker-compose restart api-gateway

# Просмотр логов
docker-compose logs -f

# Выполнение команды в контейнере
docker-compose exec api-gateway python manage.py --action stats
```

### Celery

```bash
# Просмотр активных задач
docker-compose exec celery-worker celery -A app.celery_app inspect active

# Просмотр зарегистрированных задач
docker-compose exec celery-worker celery -A app.celery_app inspect registered

# Статистика
docker-compose exec celery-worker celery -A app.celery_app inspect stats

# Очистка очереди
docker-compose exec celery-worker celery -A app.celery_app purge
```

### Управление данными

```bash
# Резервное копирование
python manage.py --action backup

# Экспорт данных
python manage.py --action export --format json

# Очистка старых уведомлений
python manage.py --action clean-notifications --days 30

# Валидация турниров
python manage.py --action validate

# Статистика
python manage.py --action stats
```

## Масштабирование

### Горизонтальное масштабирование

```bash
# Увеличение количества worker'ов
docker-compose up -d --scale celery-worker=4

# Запуск нескольких инстансов API Gateway
docker-compose up -d --scale api-gateway=3
```

### Миграция на PostgreSQL

```bash
# 1. Установите PostgreSQL
# 2. Создайте базу данных
# 3. Обновите .env
DATABASE_URL=postgresql://user:password@localhost:5432/chess_calendar

# 4. Запустите миграцию
python manage.py migrate
```

## 🤝 Участие в разработке

1. Сделайте fork репозитория
2. Создайте ветку для новой функции: `git checkout -b feature/new-feature`
3. Зафиксируйте изменения: `git commit -m 'Add new feature'`
4. Отправьте ветку: `git push origin feature/new-feature`
5. Создайте pull request

## © Авторство

Проект создан Дуплеем Максимом Игоревичем в рамках образовательного проекта.

## 📞 Контакты

Для вопросов и предложений обращайтесь по адресу: [ваш email]