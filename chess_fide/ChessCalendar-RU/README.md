# Chess Calendar RU

Шахматный календарь России - приложение для отслеживания шахматных турниров в России.

## 📋 Описание проекта

Chess Calendar RU - это веб-приложение на основе Flask, которое собирает информацию о шахматных турнирах в России с официальных источников (FIDE и Российская Федерация Шахмат), предоставляет удобный интерфейс для просмотра, фильтрации и поиска турниров, а также включает админ-панель для управления данными.

## ✨ Основные возможности

- **Автоматический парсинг турниров**: Регулярное получение информации о турнирах с FIDE и Российской Федерации Шахмат
- **Интерактивный календарь**: Удобное отображение турниров с возможностью фильтрации по различным критериям
- **Админ-панель**: Защищенная панель управления с аутентификацией для добавления, редактирования и удаления турниров
- **PWA поддержка**: Возможность установки как приложения на мобильные устройства
- **Кэширование**: Эффективное кэширование для ускорения загрузки данных
- **Уведомления**: Система уведомлений о новых турнирах и изменениях
- **API**: REST API для интеграции с другими системами
- **Экспорт данных**: Возможность экспорта турниров в CSV и JSON форматы
- **Персонализированные рекомендации**: Интеллектуальная система рекомендаций на основе предпочтений пользователей
- **Аналитика**: Комплексная система аналитики для отслеживания использования и эффективности
- **Безопасность**: Защита от DDoS-атак с помощью ограничения частоты запросов (rate limiting)
- **Продвинутые функции рекомендаций**: Коллаборативная фильтрация и трендинги на основе взаимодействий пользователей
- **Улучшенная безопасность**: Более строгая проверка паролей, блокировка аккаунтов после неудачных попыток входа
- **CSRF защита**: Защита от межсайтовой подделки запросов

## 🛠️ Технологии

- **Python 3.x**
- **Flask** - веб-фреймворк
- **Flask-SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - основная база данных (возможность переключения на PostgreSQL/MySQL)
- **BeautifulSoup4** - для парсинга HTML
- **Requests** - для HTTP-запросов
- **Bootstrap 5** - фронтенд фреймворк
- **Redis** - для кэширования (опционально)
- **Schedule** - для планирования задач

## 📦 Установка и запуск

### Требования

- Python 3.7+
- pip

### Установка зависимостей

```
pip install -r requirements.txt
```

### Настройка конфигурации

Создайте или измените файл `.env` для настройки переменных окружения:

```
SECRET_KEY=your_secret_key_here
DATABASE_URL=sqlite:///chess_calendar.db
REDIS_URL=redis://localhost:6379/0
```

### Запуск приложения

```
python run.py
```

Приложение будет доступно по адресу: http://127.0.0.1:5000

## 🔐 Административная панель

Для доступа к административной панели:

1. Создайте учетную запись администратора (см. раздел "Создание администратора")
2. Перейдите по адресу: `/admin/login`
3. Войдите с вашими учетными данными

### Создание администратора

Для создания администратора используйте следующий код в Python shell:

```
from app import create_app, db
from app.models.user import User

app = create_app()

with app.app_context():
    # Создаем администратора
    admin = User(username='admin', email='admin@example.com', password='your_secure_password', is_admin=True)
    db.session.add(admin)
    db.session.commit()
    print(f"Admin user created with API key: {admin.api_key}")
```

## 📊 Структура базы данных

### Таблица турниров

- `id` - уникальный идентификатор
- `name` - название турнира
- `start_date` - дата начала
- `end_date` - дата окончания
- `location` - место проведения
- `category` - категория (FIDE, National, Regional и т.д.)
- `status` - статус (Scheduled, Ongoing, Completed)
- `fide_id` - идентификатор турнира в системе FIDE
- `source_url` - URL источника
- `created_at` - дата создания
- `updated_at` - дата последнего обновления

### Таблица пользователей

- `id` - уникальный идентификатор
- `username` - имя пользователя
- `email` - email пользователя
- `password_hash` - хэш пароля
- `is_admin` - флаг администратора
- `created_at` - дата создания
- `last_login` - дата последнего входа
- `is_active` - статус активности
- `api_key` - API ключ для доступа к API

## 🚀 Автоматическое обновление

Приложение автоматически обновляет данные о турнирах:

- Ежедневно в 03:00
- Каждые 6 часов

Также можно вручную обновить данные через:

- Админ-панель -> "Обновить" в навигационной панели
- API endpoint: `/update`

## 📱 PWA возможности

Приложение поддерживает режим Progressive Web App (PWA):

- Возможность установки на мобильные устройства
- Офлайн режим (ограниченный)
- Push-уведомления (в разработке)
- Автономный режим работы

## 🔧 API

Приложение предоставляет REST API:

- `GET /api/tournaments` - получить все турниры
- `GET /api/tournaments/<id>` - получить турнир по ID
- `GET /api/tournaments/search` - поиск турниров
- `GET /api/recommendations/user/<user_id>` - получить рекомендации для пользователя
- `GET /api/analytics/report` - получить полный отчет аналитики
- `GET /api/analytics/tournament/<tournament_id>` - получить аналитику по турниру
- `GET /api/health` - проверка состояния системы
- `GET /admin/api/health` - проверка состояния системы
- `GET /admin/api/metrics` - получить метрики производительности
- `GET /health` - общая проверка состояния приложения
- `GET /api/tournaments/trending` - получить трендовые турниры на основе активности пользователей
- `GET /api/metrics/performance` - получить метрики производительности приложения
- `GET /api/users/<user_id>/preferences` - получить предпочтения пользователя
- `POST /api/users/<user_id>/preferences` - обновить предпочтения пользователя
- `POST /api/users/<user_id>/interactions` - записать взаимодействие пользователя с турниром
- `GET /api/users/<user_id>/recommendations/collaborative` - получить рекомендации на основе коллаборативной фильтрации

## 🔒 Безопасность

- Аутентификация администраторов с использованием сессий
- Хранение паролей с использованием безопасного хэширования
- Валидация входных данных
- Защита от SQL-инъекций через ORM
- Ограничение частоты запросов (rate limiting) для защиты от DDoS-атак
- Заголовки безопасности (X-Content-Type-Options, X-Frame-Options, CSP и др.)
- Проверка силы паролей (длина, регистр, специальные символы)
- Блокировка аккаунтов после нескольких неудачных попыток входа
- CSRF защита для предотвращения межсайтовой подделки запросов
- Расширенные заголовки безопасности (HSTS, Referrer-Policy, Permissions-Policy и др.)

## 📈 Мониторинг и логирование

- Система логирования событий и ошибок
- Мониторинг производительности
- Проверка состояния системы (health checks)
- Статистика использования
- Отслеживание времени выполнения запросов
- Выявление медленных эндпоинтов
- Сбор метрик производительности с возможностью просмотра через API

## 🛡️ Обработка ошибок

- Комплексная система обработки ошибок 404 и 500
- Логирование исключений
- Уведомления об ошибках администратору
- Автоматическая генерация отчетов об ошибках

Приложение поддерживает режим Progressive Web App (PWA):

- Возможность установки на мобильные устройства
- Офлайн режим (ограниченный)
- Push-уведомления (в разработке)
- Автономный режим работы

## 🔧 API

Приложение предоставляет REST API:

- `GET /api/tournaments` - получить все турниры
- `GET /api/tournaments/<id>` - получить турнир по ID
- `GET /admin/api/health` - проверка состояния системы
- `GET /admin/api/metrics` - получить метрики производительности

## 🔒 Безопасность

- Аутентификация администраторов с использованием сессий
- Хранение паролей с использованием безопасного хэширования
- Валидация входных данных
- Защита от SQL-инъекций через ORM

## 📈 Мониторинг и логирование

- Система логирования событий и ошибок
- Мониторинг производительности
- Проверка состояния системы (health checks)
- Статистика использования

## 🧪 Тестирование

Для запуска тестов:

```
python -m pytest tests/
```

## 🤝 Участие в разработке

1. Сделайте fork репозитория
2. Создайте ветку для новой функции: `git checkout -b feature/new-feature`
3. Зафиксируйте изменения: `git commit -m 'Add new feature'`
4. Отправьте ветку: `git push origin feature/new-feature`
5. Создайте pull request

## © Авторство

Проект создан Дуплеем Максимом Игоревичем в рамках образовательного проекта.

## 📞 Контакты

Для вопросов и предложений обращайтесь по адресу: [ваш email]