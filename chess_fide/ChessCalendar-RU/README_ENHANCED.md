# ChessCalendar-RU - Улучшенная версия

Многосервисное веб-приложение для отслеживания шахматных турниров в России с расширенными возможностями.

## 🏗️ Архитектура

### Микросервисы:

1. **Tournament Service** (`http://localhost:5001`)
   - Управление турнирами
   - CRUD операции с турнирами
   - Асинхронная обработка задач через Celery

2. **User Service** (`http://localhost:5002`)
   - Управление пользователями
   - Аутентификация и авторизация
   - Профили пользователей

3. **Parser Service** (`http://localhost:5003`)
   - Парсинг данных с FIDE и CFR
   - Автоматическое обновление турниров
   - Обработка антибот-мер

4. **CDN Service** (`http://localhost:5004`)
   - Обслуживание статических файлов
   - Интеграция с CDN провайдерами
   - Оптимизация кэширования

5. **Image Optimization Service** (`http://localhost:5005`)
   - Автоматическая оптимизация изображений
   - Конвертация форматов
   - Сжатие с сохранением качества

6. **Calendar Integration Service** (`http://localhost:5006`)
   - Экспорт турниров в календари
   - Поддержка Google Calendar и Outlook
   - Генерация ICS файлов

7. **Push Notification Service** (`http://localhost:5007`)
   - Веб-уведомления через Push API
   - Подписка/отписка пользователей
   - Массовая рассылка уведомлений

8. **ML Recommendation Service** (`http://localhost:5008`)
   - Персонализированные рекомендации
   - Контент-базированная фильтрация
   - Коллаборативная фильтрация
   - Гибридные рекомендации

## 🚀 Быстрый старт

### Требования:
- Docker и Docker Compose
- Python 3.8+

### Запуск:

```bash
# Windows
start-services.bat

# Linux/Mac
chmod +x start-services.sh
./start-services.sh
```

### Доступ к сервисам:
- **Основное приложение**: http://localhost
- **Tournament API**: http://localhost:5001
- **User API**: http://localhost:5002
- **Parser API**: http://localhost:5003
- **CDN Service**: http://localhost:5004
- **Image Optimization**: http://localhost:5005
- **Calendar Integration**: http://localhost:5006
- **Push Notifications**: http://localhost:5007
- **ML Recommendations**: http://localhost:5008

## 📊 Производительность

### Оптимизации:
- **HTTP/2** - мультиплексирование запросов
- **Gzip сжатие** - уменьшение размера передаваемых данных
- **CDN** - ускорение доставки статических файлов
- **Кэширование** - Redis для временных данных
- **Оптимизация изображений** - автоматическое сжатие
- **Асинхронная обработка** - Celery для фоновых задач

### Тестирование производительности:
```bash
# Проверка состояния всех сервисов
docker-compose ps

# Мониторинг ресурсов
docker stats

# Логи сервисов
docker-compose logs [service-name]
```

## 🛠️ Разработка

### Структура проекта:
```
ChessCalendar-RU/
├── services/
│   ├── tournament-service/
│   ├── user-service/
│   ├── parser-service/
│   ├── cdn-service/
│   ├── image-optimization-service/
│   ├── calendar-service/
│   ├── notification-service/
│   └── recommendation-service/
├── static/
├── templates/
├── docker-compose.yml
├── nginx.conf
└── nginx-performance.conf
```

### Добавление нового сервиса:
1. Создать директорию в `services/`
2. Добавить `app.py` и `requirements.txt`
3. Обновить `docker-compose.yml`
4. Добавить маршруты в `nginx.conf`

## 🔧 Конфигурация

### Переменные окружения:
```bash
# CDN
CDN_PROVIDER=cloudflare
CLOUDFLARE_API_KEY=your-api-key
CLOUDFLARE_ZONE_ID=your-zone-id

# Базы данных
DATABASE_URL=sqlite:///tournaments.db

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
```

## 📈 Мониторинг

### Health Checks:
Каждый сервис предоставляет endpoint `/health` для проверки состояния.

### Метрики:
- Время отклика API
- Количество активных пользователей
- Статистика парсинга
- Использование ресурсов

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Сделайте коммиты с описательными сообщениями
4. Отправьте pull request

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 🆘 Поддержка

- Документация API: `/api/docs`
- Примеры использования: `services/*/examples.py`
- Логи: `docker-compose logs`

---
*ChessCalendar-RU - Ваш проводник в мире шахматных турниров! ♟️*